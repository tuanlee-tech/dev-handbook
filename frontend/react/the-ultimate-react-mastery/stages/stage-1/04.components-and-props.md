# 📚 NGÀY 4: Components & Props

## 📍 Phase 1, Week 1, Day 4 of 169

## ⏱️ Thời lượng: 3-4 giờ (bao gồm breaks)

---

## I. KNOWLEDGE CHECK-IN

### ✅ Kiến thức từ Ngày 1-3:

- **Ngày 1**: ES6+ basics (destructuring, spread, arrow functions, array methods)
- **Ngày 2**: Async/await, modules, optional chaining
- **Ngày 3**: JSX syntax, conditional rendering, expressions trong JSX

### 🎯 Hôm nay sẽ học:

1. Function Components - Tạo components
2. Props - Truyền data từ parent → child
3. Props destructuring - Clean syntax
4. Children prop - Composition pattern
5. List rendering - `.map()` với key prop
6. Props validation - PropTypes (optional)

### 🔗 Ngày mai sẽ dùng để:

- Event handling trong components
- Component interaction
- Lifting state up (preview)

### ⚠️ QUAN TRỌNG - GIỚI HẠN KIẾN THỨC:

```
✅ ĐƯỢC DÙNG HÔM NAY:
- Function components
- Props (read-only data)
- Children prop
- List rendering với .map()
- Conditional rendering (từ Ngày 3)

❌ KHÔNG ĐƯỢC DÙNG HÔM NAY:
- State (chờ Ngày 6)
- Events/handlers (chờ Ngày 5)
- Hooks (chờ Week 2+)
- Class components (không học trong course này)
- Component lifecycle (học sau với useEffect)
```

---

## II. MENTAL MODELS (15 phút)

### 🧠 Big Picture: What are Components?

```
Traditional Web Development       React Components
─────────────────────────        ────────────────
index.html                       App.jsx
├─ <header>                      ├─ <Header />
├─ <nav>                         ├─ <Navigation />
├─ <main>                        ├─ <MainContent>
│  ├─ <article>                  │  ├─ <Article />
│  └─ <aside>                    │  └─ <Sidebar />
└─ <footer>                      └─ <Footer />

Monolithic HTML                  Reusable Components
Hard to maintain                 Easy to maintain
Duplicate code                   DRY (Don't Repeat Yourself)
```

### 🎯 Component Concept:

```
Component = JavaScript Function that returns JSX
────────────────────────────────────────────────

Like a JavaScript function:
- Has a name (PascalCase)
- Can accept inputs (props)
- Returns output (JSX)

function Greeting(props) {
  return <h1>Hello, {props.name}!</h1>
}

Usage:
<Greeting name="John" />
```

### 🔄 Props Flow:

```
Parent Component
    │
    │ passes props (data)
    ↓
Child Component
    │
    │ uses props (read-only)
    ↓
Renders UI


Example:
────────
<UserCard
  name="John"          ← Props
  age={25}
  email="john@..."
/>

function UserCard(props) {
  return (
    <div>
      <h2>{props.name}</h2>      ← Use props
      <p>{props.age}</p>
      <p>{props.email}</p>
    </div>
  )
}

IMPORTANT: Props are READ-ONLY!
Child CANNOT modify props
Data flows ONE WAY: Parent → Child
```

---

## III. CONTENT DEEP DIVE

## 📌 1. FUNCTION COMPONENTS (30 phút)

### Tạo Component Đầu Tiên:

```javascript
// ═══════════════════════════════════════════════════════════
// Component đơn giản nhất
// ═══════════════════════════════════════════════════════════

// Cách 1: Function declaration
function Greeting() {
  return <h1>Hello, World!</h1>;
}

// Cách 2: Arrow function (recommended)
const Greeting = () => {
  return <h1>Hello, World!</h1>;
};

// Cách 3: Arrow function với implicit return
const Greeting = () => <h1>Hello, World!</h1>;

// Sử dụng component:
function App() {
  return (
    <div>
      <Greeting />
      <Greeting />
      <Greeting />
    </div>
  );
}
```

### Component Naming Rules:

```javascript
// ═══════════════════════════════════════════════════════════
// RULES: Component names phải PascalCase
// ═══════════════════════════════════════════════════════════

// ✅ ĐÚNG: PascalCase
function UserProfile() {}
function ProductCard() {}
function NavigationMenu() {}

// ❌ SAI: camelCase hoặc lowercase
function userProfile() {} // React nghĩ đây là HTML tag
function productcard() {} // React nghĩ đây là HTML tag

// ✅ ĐÚNG: Multi-word components
function UserProfileCard() {}
function ShoppingCartItem() {}

// ❌ SAI: Single letter (trừ khi convention đặc biệt)
function U() {}
function P() {}
```

### ❌ Anti-patterns:

```javascript
// ❌ WRONG: Component không return gì
function BrokenComponent() {
  const message = "Hello"
  // Quên return!
}

// ❌ WRONG: Return undefined
function BrokenComponent() {
  return  // Nothing after return
  <div>Hello</div>
}

// ❌ WRONG: Multiple root elements không wrap
function BrokenComponent() {
  return (
    <h1>Title</h1>
    <p>Paragraph</p>  // Error!
  )
}

// ❌ WRONG: Conditional return phức tạp
function BrokenComponent() {
  if (condition1) {
    return <div>A</div>
  } else if (condition2) {
    return <div>B</div>
  } else if (condition3) {
    return <div>C</div>
  } else {
    return <div>D</div>
  }
  // Hard to read!
}

// ❌ WRONG: Nested component definitions
function ParentComponent() {
  // ❌ Không định nghĩa component trong component khác!
  function ChildComponent() {
    return <div>Child</div>
  }

  return <ChildComponent />
}
```

### ✅ Best Practices:

```javascript
// ═══════════════════════════════════════════════════════════
// Component organization
// ═══════════════════════════════════════════════════════════

// ✅ One component per file (recommended)
// File: Button.jsx
const Button = () => {
  return <button>Click me</button>
}

export default Button

// ✅ Import và sử dụng
import Button from './Button'

function App() {
  return <Button />
}

// ═══════════════════════════════════════════════════════════
// Multiple components trong một file (chỉ khi liên quan chặt)
// ═══════════════════════════════════════════════════════════

// File: Card.jsx
const CardHeader = () => {
  return <div className="card-header">Header</div>
}

const CardBody = () => {
  return <div className="card-body">Body</div>
}

const CardFooter = () => {
  return <div className="card-footer">Footer</div>
}

// Main component
const Card = () => {
  return (
    <div className="card">
      <CardHeader />
      <CardBody />
      <CardFooter />
    </div>
  )
}

export default Card

// ═══════════════════════════════════════════════════════════
// Component với logic bên trong
// ═══════════════════════════════════════════════════════════

const UserGreeting = () => {
  // Có thể có variables
  const currentHour = new Date().getHours()

  // Có thể có functions
  const getTimeOfDay = () => {
    if (currentHour < 12) return 'morning'
    if (currentHour < 18) return 'afternoon'
    return 'evening'
  }

  // Có thể có computations
  const timeOfDay = getTimeOfDay()
  const greeting = `Good ${timeOfDay}!`

  return (
    <div>
      <h1>{greeting}</h1>
      <p>Current time: {currentHour}:00</p>
    </div>
  )
}

// ═══════════════════════════════════════════════════════════
// Early returns cho conditional rendering
// ═══════════════════════════════════════════════════════════

const UserStatus = () => {
  const isLoggedIn = false

  // ✅ Early return pattern
  if (!isLoggedIn) {
    return <div>Please log in</div>
  }

  // Main content
  return (
    <div>
      <h1>Welcome back!</h1>
      <p>Your dashboard</p>
    </div>
  )
}
```

### Component Composition:

```javascript
// ═══════════════════════════════════════════════════════════
// Building complex UIs từ simple components
// ═══════════════════════════════════════════════════════════

// Small components
const Avatar = () => {
  return <img src="avatar.jpg" alt="User" className="avatar" />;
};

const UserName = () => {
  return <h3 className="user-name">John Doe</h3>;
};

const UserEmail = () => {
  return <p className="user-email">john@example.com</p>;
};

// Compose thành larger component
const UserProfile = () => {
  return (
    <div className="user-profile">
      <Avatar />
      <div className="user-info">
        <UserName />
        <UserEmail />
      </div>
    </div>
  );
};

// Sử dụng trong App
const App = () => {
  return (
    <div className="app">
      <UserProfile />
      <UserProfile />
      <UserProfile />
    </div>
  );
};
```

---

## 📌 2. PROPS - PASSING DATA (45 phút)

### Props Basics:

```javascript
// ═══════════════════════════════════════════════════════════
// Passing props từ parent sang child
// ═══════════════════════════════════════════════════════════

// Child component nhận props
function Greeting(props) {
  return <h1>Hello, {props.name}!</h1>;
}

// Parent component truyền props
function App() {
  return (
    <div>
      <Greeting name="John" />
      <Greeting name="Jane" />
      <Greeting name="Bob" />
    </div>
  );
}

// Renders:
// Hello, John!
// Hello, Jane!
// Hello, Bob!
```

### Props Types:

```javascript
// ═══════════════════════════════════════════════════════════
// Các kiểu data có thể pass qua props
// ═══════════════════════════════════════════════════════════

function ComponentWithProps(props) {
  return (
    <div>
      {/* String prop */}
      <p>String: {props.stringProp}</p>

      {/* Number prop */}
      <p>Number: {props.numberProp}</p>

      {/* Boolean prop */}
      <p>Boolean: {props.booleanProp.toString()}</p>

      {/* Array prop */}
      <p>Array: {props.arrayProp.join(", ")}</p>

      {/* Object prop */}
      <p>Object: {props.objectProp.name}</p>

      {/* Function prop (sẽ học kỹ Ngày 5) */}
      <p>Function: {props.functionProp()}</p>
    </div>
  );
}

// Usage:
function App() {
  const user = { name: "John", age: 25 };
  const numbers = [1, 2, 3, 4, 5];

  return (
    <ComponentWithProps
      stringProp="Hello" // String - không cần {}
      numberProp={42} // Number - cần {}
      booleanProp={true} // Boolean - cần {}
      arrayProp={numbers} // Array - cần {}
      objectProp={user} // Object - cần {}
      functionProp={() => "Hi"} // Function - cần {}
    />
  );
}

// ═══════════════════════════════════════════════════════════
// Rule: Chỉ có strings không cần {}, tất cả khác cần {}
// ═══════════════════════════════════════════════════════════
```

### Props Default Values:

```javascript
// ═══════════════════════════════════════════════════════════
// Setting default values cho props
// ═══════════════════════════════════════════════════════════

// Cách 1: Inline default value
function Greeting(props) {
  const name = props.name || 'Guest'
  return <h1>Hello, {name}!</h1>
}

// Cách 2: Nullish coalescing (better)
function Greeting(props) {
  const name = props.name ?? 'Guest'
  return <h1>Hello, {name}!</h1>
}

// Cách 3: Destructuring với default (best)
function Greeting({ name = 'Guest' }) {
  return <h1>Hello, {name}!</h1>
}

// Usage:
<Greeting name="John" />  // Hello, John!
<Greeting />              // Hello, Guest!
```

### ❌ Anti-patterns với Props:

```javascript
// ❌ WRONG: Trying to modify props
function BrokenComponent(props) {
  props.name = "Modified"; // ERROR! Props are read-only!
  return <h1>{props.name}</h1>;
}

// ❌ WRONG: Mutating object props
function BrokenComponent(props) {
  props.user.name = "Modified"; // BAD! Mutates parent's data
  return <h1>{props.user.name}</h1>;
}

// ❌ WRONG: Too many props (hard to maintain)
function OverloadedComponent(props) {
  return (
    <div>
      <p>{props.name}</p>
      <p>{props.age}</p>
      <p>{props.email}</p>
      <p>{props.phone}</p>
      <p>{props.address}</p>
      <p>{props.city}</p>
      <p>{props.country}</p>
      <p>{props.zipCode}</p>
      {/* 20 more props... */}
    </div>
  );
}

// ✅ BETTER: Group related props trong object
function BetterComponent(props) {
  return (
    <div>
      <p>{props.user.name}</p>
      <p>{props.user.age}</p>
      <p>{props.contact.email}</p>
      <p>{props.contact.phone}</p>
      <p>{props.address.city}</p>
      <p>{props.address.country}</p>
    </div>
  );
}

// Usage:
<BetterComponent
  user={{ name: "John", age: 25 }}
  contact={{ email: "...", phone: "..." }}
  address={{ city: "...", country: "..." }}
/>;

// ❌ WRONG: Prop drilling (passing props through nhiều levels)
// Sẽ học cách giải quyết với Context API sau
```

### ✅ Props Best Practices:

```javascript
// ═══════════════════════════════════════════════════════════
// Pattern 1: Simple props
// ═══════════════════════════════════════════════════════════

function ProductCard(props) {
  return (
    <div className="product-card">
      <h3>{props.name}</h3>
      <p className="price">${props.price}</p>
      <p className="description">{props.description}</p>
    </div>
  )
}

// Usage:
<ProductCard
  name="Laptop"
  price={999}
  description="High-performance laptop"
/>

// ═══════════════════════════════════════════════════════════
// Pattern 2: Object props cho grouped data
// ═══════════════════════════════════════════════════════════

function UserCard(props) {
  return (
    <div className="user-card">
      <h3>{props.user.name}</h3>
      <p>{props.user.email}</p>
      <p>{props.user.role}</p>
    </div>
  )
}

// Usage:
const user = {
  name: 'John Doe',
  email: 'john@example.com',
  role: 'Developer'
}

<UserCard user={user} />

// ═══════════════════════════════════════════════════════════
// Pattern 3: Boolean props
// ═══════════════════════════════════════════════════════════

function Button(props) {
  return (
    <button
      disabled={props.disabled}
      className={props.primary ? 'btn-primary' : 'btn-secondary'}
    >
      {props.label}
    </button>
  )
}

// Usage - shorthand cho true:
<Button label="Click" disabled />           // disabled={true}
<Button label="Click" primary />            // primary={true}
<Button label="Click" disabled={false} />   // Explicitly false

// ═══════════════════════════════════════════════════════════
// Pattern 4: Conditional props
// ═══════════════════════════════════════════════════════════

function Alert(props) {
  // Determine style dựa trên type
  const alertClass = `alert alert-${props.type}`

  // Determine icon dựa trên type
  const getIcon = () => {
    const icons = {
      success: '✓',
      error: '✗',
      warning: '⚠',
      info: 'ℹ'
    }
    return icons[props.type] || 'ℹ'
  }

  return (
    <div className={alertClass}>
      <span className="icon">{getIcon()}</span>
      <p>{props.message}</p>
    </div>
  )
}

// Usage:
<Alert type="success" message="Operation successful!" />
<Alert type="error" message="Something went wrong" />
<Alert type="warning" message="Please be careful" />

// ═══════════════════════════════════════════════════════════
// Pattern 5: Computed props
// ═══════════════════════════════════════════════════════════

function PriceDisplay(props) {
  // Calculate discount
  const discount = props.originalPrice - props.price
  const discountPercent = ((discount / props.originalPrice) * 100).toFixed(0)
  const hasDiscount = discount > 0

  return (
    <div className="price-display">
      <p className="current-price">${props.price}</p>

      {hasDiscount && (
        <>
          <p className="original-price">${props.originalPrice}</p>
          <p className="discount">Save ${discount} ({discountPercent}% off)</p>
        </>
      )}
    </div>
  )
}

// Usage:
<PriceDisplay price={299} originalPrice={399} />
```

---

## 📌 3. PROPS DESTRUCTURING (30 phút)

### Destructuring Basics:

```javascript
// ═══════════════════════════════════════════════════════════
// Without destructuring (verbose)
// ═══════════════════════════════════════════════════════════

function UserCard(props) {
  return (
    <div>
      <h2>{props.name}</h2>
      <p>{props.email}</p>
      <p>{props.role}</p>
    </div>
  );
}

// ═══════════════════════════════════════════════════════════
// With destructuring trong function body
// ═══════════════════════════════════════════════════════════

function UserCard(props) {
  const { name, email, role } = props;

  return (
    <div>
      <h2>{name}</h2>
      <p>{email}</p>
      <p>{role}</p>
    </div>
  );
}

// ═══════════════════════════════════════════════════════════
// With destructuring trong parameters (recommended)
// ═══════════════════════════════════════════════════════════

function UserCard({ name, email, role }) {
  return (
    <div>
      <h2>{name}</h2>
      <p>{email}</p>
      <p>{role}</p>
    </div>
  );
}

// All three are equivalent!
```

### Destructuring với Default Values:

```javascript
// ═══════════════════════════════════════════════════════════
// Default values trong destructuring
// ═══════════════════════════════════════════════════════════

function Button({
  label = 'Click me',           // Default string
  disabled = false,             // Default boolean
  type = 'button',              // Default string
  size = 'medium'               // Default string
}) {
  return (
    <button
      disabled={disabled}
      type={type}
      className={`btn btn-${size}`}
    >
      {label}
    </button>
  )
}

// Usage:
<Button />                              // All defaults
<Button label="Submit" />               // Override một prop
<Button label="Delete" size="large" /> // Override nhiều props
```

### Nested Destructuring:

```javascript
// ═══════════════════════════════════════════════════════════
// Destructuring nested objects
// ═══════════════════════════════════════════════════════════

// Data structure:
const user = {
  name: "John",
  age: 25,
  address: {
    city: "New York",
    country: "USA",
    coordinates: {
      lat: 40.7128,
      lng: -74.006,
    },
  },
};

// Cách 1: Destructure từng level
function UserLocation({ address }) {
  const { city, country } = address;

  return (
    <div>
      <p>
        {city}, {country}
      </p>
    </div>
  );
}

// Cách 2: Nested destructuring (careful - can be hard to read)
function UserLocation({ address: { city, country } }) {
  return (
    <div>
      <p>
        {city}, {country}
      </p>
    </div>
  );
}

// Cách 3: Deep nested destructuring (use sparingly)
function UserCoordinates({
  address: {
    coordinates: { lat, lng },
  },
}) {
  return (
    <p>
      Lat: {lat}, Lng: {lng}
    </p>
  );
}

// ⚠️ Nested destructuring quá sâu hard to read!
// ✅ Better: Destructure trong function body khi cần
function UserInfo({ address }) {
  const { city, country, coordinates } = address;
  const { lat, lng } = coordinates;

  return (
    <div>
      <p>
        {city}, {country}
      </p>
      <p>
        Coordinates: {lat}, {lng}
      </p>
    </div>
  );
}
```

### Destructuring với Rest:

```javascript
// ═══════════════════════════════════════════════════════════
// Using rest operator với destructuring
// ═══════════════════════════════════════════════════════════

function UserCard({ name, email, ...otherProps }) {
  console.log(otherProps); // { age: 25, role: 'Developer', ... }

  return (
    <div>
      <h2>{name}</h2>
      <p>{email}</p>
      <pre>{JSON.stringify(otherProps, null, 2)}</pre>
    </div>
  );
}

// Usage:
<UserCard
  name="John"
  email="john@example.com"
  age={25}
  role="Developer"
  location="New York"
/>;

// ═══════════════════════════════════════════════════════════
// Spreading remaining props (common pattern)
// ═══════════════════════════════════════════════════════════

function Input({ label, ...inputProps }) {
  return (
    <div className="input-group">
      <label>{label}</label>
      {/* Spread all remaining props vào input */}
      <input {...inputProps} />
    </div>
  );
}

// Usage - all HTML input attributes pass through:
<Input
  label="Email"
  type="email"
  placeholder="Enter email"
  required
  maxLength={50}
/>;

// Renders:
// <div class="input-group">
//   <label>Email</label>
//   <input
//     type="email"
//     placeholder="Enter email"
//     required
//     maxlength="50"
//   />
// </div>
```

### ✅ Destructuring Best Practices:

```javascript
// ═══════════════════════════════════════════════════════════
// Pattern 1: Rename props khi destructuring
// ═══════════════════════════════════════════════════════════

function Component({
  name: userName, // Rename name → userName
  email: userEmail, // Rename email → userEmail
}) {
  return (
    <div>
      <p>Name: {userName}</p>
      <p>Email: {userEmail}</p>
    </div>
  );
}

// ═══════════════════════════════════════════════════════════
// Pattern 2: Selective destructuring
// ═══════════════════════════════════════════════════════════

function ProductCard(props) {
  // Destructure chỉ những gì cần trong function body
  const { name, price } = props;

  // Vẫn có thể access props.something khác
  console.log(props);

  return (
    <div>
      <h3>{name}</h3>
      <p>${price}</p>
    </div>
  );
}

// ═══════════════════════════════════════════════════════════
// Pattern 3: Multi-line destructuring cho readability
// ═══════════════════════════════════════════════════════════

function ComplexComponent({
  title,
  subtitle,
  description,
  imageUrl,
  author,
  publishDate,
  category,
  tags,
  readTime,
}) {
  return (
    <article>
      <h1>{title}</h1>
      <h2>{subtitle}</h2>
      {/* ... */}
    </article>
  );
}

// ═══════════════════════════════════════════════════════════
// Pattern 4: Conditional destructuring
// ═══════════════════════════════════════════════════════════

function UserProfile({ user }) {
  // Destructure only if user exists
  if (!user) {
    return <div>No user data</div>;
  }

  const { name, email, avatar } = user;

  return (
    <div>
      <img src={avatar} alt={name} />
      <h2>{name}</h2>
      <p>{email}</p>
    </div>
  );
}

// Or với optional chaining:
function UserProfile({ user }) {
  const name = user?.name ?? "Unknown";
  const email = user?.email ?? "No email";

  return (
    <div>
      <h2>{name}</h2>
      <p>{email}</p>
    </div>
  );
}
```

---

## 📌 4. CHILDREN PROP (30 phút)

### Children Prop Basics:

```javascript
// ═══════════════════════════════════════════════════════════
// Children prop là special prop
// ═══════════════════════════════════════════════════════════

// Wrapper component nhận children
function Card({ children }) {
  return <div className="card">{children}</div>;
}

// Usage: Anything giữa opening và closing tags là children
function App() {
  return (
    <Card>
      <h2>This is the title</h2>
      <p>This is the content</p>
    </Card>
  );
}

// Renders:
// <div class="card">
//   <h2>This is the title</h2>
//   <p>This is the content</p>
// </div>
```

### Children Types:

```javascript
// ═══════════════════════════════════════════════════════════
// Children có thể là bất kỳ JSX nào
// ═══════════════════════════════════════════════════════════

function Container({ children }) {
  return <div className="container">{children}</div>
}

// 1. Text children
<Container>
  Plain text content
</Container>

// 2. Single element children
<Container>
  <h1>Title</h1>
</Container>

// 3. Multiple elements children
<Container>
  <h1>Title</h1>
  <p>Paragraph 1</p>
  <p>Paragraph 2</p>
</Container>

// 4. Component children
<Container>
  <Header />
  <MainContent />
  <Footer />
</Container>

// 5. Mixed children
<Container>
  <h1>Title</h1>
  Some text
  <p>Paragraph</p>
  <Button />
</Container>

// 6. Expression children
<Container>
  {users.map(user => (
    <UserCard key={user.id} user={user} />
  ))}
</Container>
```

### Layout Components với Children:

```javascript
// ═══════════════════════════════════════════════════════════
// Pattern 1: Container components
// ═══════════════════════════════════════════════════════════

function PageLayout({ children }) {
  return (
    <div className="page-layout">
      <header className="header">
        <h1>My Website</h1>
      </header>

      <main className="main-content">{children}</main>

      <footer className="footer">
        <p>© 2025 My Website</p>
      </footer>
    </div>
  );
}

// Usage:
function App() {
  return (
    <PageLayout>
      <h2>Welcome to my page</h2>
      <p>This is the main content</p>
    </PageLayout>
  );
}

// ═══════════════════════════════════════════════════════════
// Pattern 2: Card components
// ═══════════════════════════════════════════════════════════

function Card({ title, children }) {
  return (
    <div className="card">
      {title && <h3 className="card-title">{title}</h3>}
      <div className="card-body">{children}</div>
    </div>
  );
}

// Usage:
<Card title="User Information">
  <p>Name: John Doe</p>
  <p>Email: john@example.com</p>
  <button>Edit Profile</button>
</Card>;

// ═══════════════════════════════════════════════════════════
// Pattern 3: Modal/Dialog components
// ═══════════════════════════════════════════════════════════

function Modal({ title, children, footer }) {
  return (
    <div className="modal-overlay">
      <div className="modal">
        <div className="modal-header">
          <h2>{title}</h2>
          <button className="close-btn">×</button>
        </div>

        <div className="modal-body">{children}</div>

        {footer && <div className="modal-footer">{footer}</div>}
      </div>
    </div>
  );
}

// Usage:
<Modal
  title="Confirm Delete"
  footer={
    <>
      <button>Cancel</button>
      <button>Delete</button>
    </>
  }
>
  <p>Are you sure you want to delete this item?</p>
  <p>This action cannot be undone.</p>
</Modal>;
```

### Combining Children với Other Props:

```javascript
// ═══════════════════════════════════════════════════════════
// Children + regular props
// ═══════════════════════════════════════════════════════════

function Alert({ type, title, children }) {
  // Icon dựa vào type
  const icons = {
    success: '✓',
    error: '✗',
    warning: '⚠',
    info: 'ℹ'
  }

  const icon = icons[type] || icons.info
  const alertClass = `alert alert-${type}`

  return (
    <div className={alertClass}>
      <div className="alert-header">
        <span className="alert-icon">{icon}</span>
        <h4>{title}</h4>
      </div>
      <div className="alert-body">
        {children}
      </div>
    </div>
  )
}

// Usage:
<Alert type="success" title="Success!">
  <p>Your changes have been saved successfully.</p>
  <p>You can now close this dialog.</p>
</Alert>

<Alert type="error" title="Error">
  <p>Something went wrong:</p>
  <ul>
    <li>Network connection failed</li>
    <li>Please try again later</li>
  </ul>
</Alert>

// ═══════════════════════════════════════════════════════════
// Wrapper với conditional styling
// ═══════════════════════════════════════════════════════════

function Section({ title, highlight, children }) {
  const sectionClass = `section ${highlight ? 'section-highlight' : ''}`

  return (
    <section className={sectionClass}>
      {title && <h2 className="section-title">{title}</h2>}
      <div className="section-content">
        {children}
      </div>
    </section>
  )
}

// Usage:
<Section title="Featured Products" highlight>
  <ProductCard name="Product 1" price={99} />
  <ProductCard name="Product 2" price={149} />
  <ProductCard name="Product 3" price={199} />
</Section>
```

### ✅ Children Best Practices:

```javascript
// ═══════════════════════════════════════════════════════════
// Pattern 1: Flexible container components
// ═══════════════════════════════════════════════════════════

function FlexContainer({ direction = 'row', gap = '1rem', children }) {
  return (
    <div
      style={{
        display: 'flex',
        flexDirection: direction,
        gap: gap
      }}
    >
      {children}
    </div>
  )
}

// Usage:
<FlexContainer direction="column" gap="2rem">
  <div>Item 1</div>
  <div>Item 2</div>
  <div>Item 3</div>
</FlexContainer>

// ═══════════════════════════════════════════════════════════
// Pattern 2: Conditional children rendering
// ═══════════════════════════════════════════════════════════

function ConditionalWrapper({ condition, wrapper, children }) {
  // Wrap children only if condition is true
  if (condition) {
    return wrapper(children)
  }
  return children
}

// Usage:
<ConditionalWrapper
  condition={isHighlighted}
  wrapper={children => <mark>{children}</mark>}
>
  This text may be highlighted
</ConditionalWrapper>

// ═══════════════════════════════════════════════════════════
// Pattern 3: Default children
// ═══════════════════════════════════════════════════════════

function EmptyState({ children }) {
  return (
    <div className="empty-state">
      {children || (
        <>
          <p>No data available</p>
          <button>Add New Item</button>
        </>
      )}
    </div>
  )
}

// Usage với custom content:
<EmptyState>
  <p>Your cart is empty</p>
  <button>Start Shopping</button>
</EmptyState>

// Usage với default content:
<EmptyState />
```

---

## 📌 5. LIST RENDERING (45 phút)

### Array.map() Basics:

```javascript
// ═══════════════════════════════════════════════════════════
// Rendering lists với .map()
// ═══════════════════════════════════════════════════════════

function UserList() {
  const users = [
    { id: 1, name: "John Doe", email: "john@example.com" },
    { id: 2, name: "Jane Smith", email: "jane@example.com" },
    { id: 3, name: "Bob Johnson", email: "bob@example.com" },
  ];

  return (
    <div>
      <h2>Users</h2>
      {users.map((user) => (
        <div key={user.id}>
          <h3>{user.name}</h3>
          <p>{user.email}</p>
        </div>
      ))}
    </div>
  );
}

// Renders:
// <div>
//   <h2>Users</h2>
//   <div><h3>John Doe</h3><p>john@example.com</p></div>
//   <div><h3>Jane Smith</h3><p>jane@example.com</p></div>
//   <div><h3>Bob Johnson</h3><p>bob@example.com</p></div>
// </div>
```

### Key Prop - VERY IMPORTANT:

```javascript
// ═══════════════════════════════════════════════════════════
// WHY keys are important
// ═══════════════════════════════════════════════════════════

// ❌ WRONG: No key (React warning)
{
  users.map((user) => (
    <div>
      <h3>{user.name}</h3>
    </div>
  ));
}
// Warning: Each child in a list should have a unique "key" prop

// ❌ WRONG: Using index as key (anti-pattern)
{
  users.map((user, index) => (
    <div key={index}>
      <h3>{user.name}</h3>
    </div>
  ));
}
// Works but can cause bugs với sorting/filtering

// ✅ RIGHT: Using unique, stable ID
{
  users.map((user) => (
    <div key={user.id}>
      <h3>{user.name}</h3>
    </div>
  ));
}

// ═══════════════════════════════════════════════════════════
// Why index as key is bad
// ═══════════════════════════════════════════════════════════

// Initial list:
// [
//   { id: 1, name: 'John' },  // index: 0, key: 0
//   { id: 2, name: 'Jane' },  // index: 1, key: 1
//   { id: 3, name: 'Bob' }    // index: 2, key: 2
// ]

// After removing middle item:
// [
//   { id: 1, name: 'John' },  // index: 0, key: 0 (same)
//   { id: 3, name: 'Bob' }    // index: 1, key: 1 (CHANGED! was key: 2)
// ]

// React thinks:
// - Item at index 0 didn't change ✓
// - Item at index 1 changed from Jane to Bob ✗ (wrong!)
// - Item at index 2 was removed ✗ (wrong!)

// With stable ID keys:
// - John (key: 1) stayed ✓
// - Jane (key: 2) was removed ✓
// - Bob (key: 3) stayed ✓
```

### List Rendering Patterns:

```javascript
// ═══════════════════════════════════════════════════════════
// Pattern 1: Simple list
// ═══════════════════════════════════════════════════════════

function FruitList() {
  const fruits = ["Apple", "Banana", "Orange", "Mango"];

  return (
    <ul>
      {fruits.map((fruit, index) => (
        // OK to use index here - list is static, no reordering
        <li key={index}>{fruit}</li>
      ))}
    </ul>
  );
}

// ═══════════════════════════════════════════════════════════
// Pattern 2: Component list
// ═══════════════════════════════════════════════════════════

function ProductGrid() {
  const products = [
    { id: 1, name: "Laptop", price: 999 },
    { id: 2, name: "Mouse", price: 29 },
    { id: 3, name: "Keyboard", price: 79 },
  ];

  return (
    <div className="product-grid">
      {products.map((product) => (
        <ProductCard
          key={product.id}
          name={product.name}
          price={product.price}
        />
      ))}
    </div>
  );
}

// ═══════════════════════════════════════════════════════════
// Pattern 3: List với index
// ═══════════════════════════════════════════════════════════

function NumberedList() {
  const items = [
    { id: 1, text: "First item" },
    { id: 2, text: "Second item" },
    { id: 3, text: "Third item" },
  ];

  return (
    <ol>
      {items.map((item, index) => (
        <li key={item.id}>
          #{index + 1}: {item.text}
        </li>
      ))}
    </ol>
  );
}

// ═══════════════════════════════════════════════════════════
// Pattern 4: Filtered list
// ═══════════════════════════════════════════════════════════

function ActiveUsersList() {
  const users = [
    { id: 1, name: "John", active: true },
    { id: 2, name: "Jane", active: false },
    { id: 3, name: "Bob", active: true },
  ];

  // Filter trước khi map
  const activeUsers = users.filter((user) => user.active);

  return (
    <div>
      <h2>Active Users ({activeUsers.length})</h2>
      {activeUsers.map((user) => (
        <div key={user.id}>{user.name}</div>
      ))}
    </div>
  );
}

// Hoặc chain trực tiếp:
function ActiveUsersList2() {
  const users = [
    { id: 1, name: "John", active: true },
    { id: 2, name: "Jane", active: false },
    { id: 3, name: "Bob", active: true },
  ];

  return (
    <div>
      {users
        .filter((user) => user.active)
        .map((user) => (
          <div key={user.id}>{user.name}</div>
        ))}
    </div>
  );
}

// ═══════════════════════════════════════════════════════════
// Pattern 5: Sorted list
// ═══════════════════════════════════════════════════════════

function SortedProductList() {
  const products = [
    { id: 1, name: "Laptop", price: 999 },
    { id: 2, name: "Mouse", price: 29 },
    { id: 3, name: "Keyboard", price: 79 },
  ];

  // Sort copy of array (don't mutate original)
  const sortedProducts = [...products].sort((a, b) => a.price - b.price);

  return (
    <div>
      <h2>Products (Price: Low to High)</h2>
      {sortedProducts.map((product) => (
        <div key={product.id}>
          {product.name} - ${product.price}
        </div>
      ))}
    </div>
  );
}
```

### Nested Lists:

```javascript
// ═══════════════════════════════════════════════════════════
// Rendering nested lists
// ═══════════════════════════════════════════════════════════

function CategoryList() {
  const categories = [
    {
      id: 1,
      name: "Electronics",
      products: [
        { id: 101, name: "Laptop" },
        { id: 102, name: "Mouse" },
      ],
    },
    {
      id: 2,
      name: "Clothing",
      products: [
        { id: 201, name: "Shirt" },
        { id: 202, name: "Pants" },
      ],
    },
  ];

  return (
    <div>
      {categories.map((category) => (
        <div key={category.id} className="category">
          <h2>{category.name}</h2>
          <ul>
            {category.products.map((product) => (
              <li key={product.id}>{product.name}</li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  );
}

// Renders:
// <div>
//   <div class="category">
//     <h2>Electronics</h2>
//     <ul>
//       <li>Laptop</li>
//       <li>Mouse</li>
//     </ul>
//   </div>
//   <div class="category">
//     <h2>Clothing</h2>
//     <ul>
//       <li>Shirt</li>
//       <li>Pants</li>
//     </ul>
//   </div>
// </div>
```

### Empty Lists:

```javascript
// ═══════════════════════════════════════════════════════════
// Handling empty lists
// ═══════════════════════════════════════════════════════════

function UserList() {
  const users = []; // Empty array

  // Pattern 1: Conditional rendering
  if (users.length === 0) {
    return (
      <div className="empty-state">
        <p>No users found</p>
        <button>Add User</button>
      </div>
    );
  }

  return (
    <div>
      {users.map((user) => (
        <div key={user.id}>{user.name}</div>
      ))}
    </div>
  );
}

// Pattern 2: Inline conditional
function UserList2() {
  const users = [];

  return (
    <div>
      {users.length === 0 ? (
        <p>No users found</p>
      ) : (
        users.map((user) => <div key={user.id}>{user.name}</div>)
      )}
    </div>
  );
}

// Pattern 3: Logical AND
function UserList3() {
  const users = [];

  return (
    <div>
      {users.length === 0 && <p>No users found</p>}

      {users.length > 0 && (
        <div>
          {users.map((user) => (
            <div key={user.id}>{user.name}</div>
          ))}
        </div>
      )}
    </div>
  );
}
```

### ❌ Common List Rendering Mistakes:

```javascript
// ❌ WRONG: Missing key
{
  products.map((product) => <ProductCard name={product.name} />);
}

// ❌ WRONG: Non-unique keys
{
  products.map((product, index) => (
    <ProductCard key={product.category} name={product.name} />
    // Multiple products can have same category!
  ));
}

// ❌ WRONG: Key on wrong element
{
  products.map((product) => (
    <div>
      <ProductCard key={product.id} name={product.name} />
    </div>
  ));
}
// Key should be on the outermost element in map!

// ✅ RIGHT:
{
  products.map((product) => (
    <div key={product.id}>
      <ProductCard name={product.name} />
    </div>
  ));
}

// ❌ WRONG: Mutating array
function SortedList() {
  const items = [3, 1, 2];
  items.sort(); // Mutates original!

  return (
    <div>
      {items.map((item, index) => (
        <div key={index}>{item}</div>
      ))}
    </div>
  );
}

// ✅ RIGHT: Copy then sort
function SortedList() {
  const items = [3, 1, 2];
  const sortedItems = [...items].sort();

  return (
    <div>
      {sortedItems.map((item, index) => (
        <div key={index}>{item}</div>
      ))}
    </div>
  );
}
```

---

## IV. LIVE CODING DEMOS (45 phút)

### 🎯 Demo 1: Blog Post List (⭐⭐)

```javascript
// Requirement: Tạo blog post list với author info và tags

// Blog post component
function BlogPost({ title, excerpt, author, date, tags, readTime }) {
  return (
    <article className="blog-post">
      <h2 className="post-title">{title}</h2>

      <div className="post-meta">
        <span className="author">By {author}</span>
        <span className="date">{date}</span>
        <span className="read-time">{readTime} min read</span>
      </div>

      <p className="post-excerpt">{excerpt}</p>

      <div className="post-tags">
        {tags.map((tag, index) => (
          <span key={index} className="tag">
            #{tag}
          </span>
        ))}
      </div>

      <button className="read-more">Read More →</button>
    </article>
  );
}

// Blog list component
function BlogList() {
  const posts = [
    {
      id: 1,
      title: "Getting Started with React",
      excerpt:
        "Learn the basics of React and start building modern web applications.",
      author: "John Doe",
      date: "Jan 1, 2025",
      tags: ["react", "javascript", "tutorial"],
      readTime: 5,
    },
    {
      id: 2,
      title: "Understanding Props and Components",
      excerpt:
        "Deep dive into React components and how to pass data using props.",
      author: "Jane Smith",
      date: "Jan 2, 2025",
      tags: ["react", "components", "props"],
      readTime: 8,
    },
    {
      id: 3,
      title: "Modern JavaScript for React",
      excerpt: "Master ES6+ features that are essential for React development.",
      author: "Bob Johnson",
      date: "Jan 3, 2025",
      tags: ["javascript", "es6", "react"],
      readTime: 10,
    },
  ];

  return (
    <div className="blog-container">
      <header className="blog-header">
        <h1>📝 Developer Blog</h1>
        <p>Latest articles about web development</p>
      </header>

      <div className="blog-list">
        {posts.map((post) => (
          <BlogPost
            key={post.id}
            title={post.title}
            excerpt={post.excerpt}
            author={post.author}
            date={post.date}
            tags={post.tags}
            readTime={post.readTime}
          />
        ))}
      </div>

      <div className="blog-stats">
        <p>Total Posts: {posts.length}</p>
        <p>
          Total Read Time:{" "}
          {posts.reduce((total, post) => total + post.readTime, 0)} minutes
        </p>
      </div>
    </div>
  );
}

export default BlogList;
```

**CSS**:

```css
.blog-container {
  max-width: 800px;
  margin: 2rem auto;
  padding: 2rem;
}

.blog-header {
  text-align: center;
  margin-bottom: 3rem;
}

.blog-header h1 {
  margin: 0 0 0.5rem 0;
  font-size: 2.5rem;
  color: #333;
}

.blog-header p {
  margin: 0;
  color: #666;
  font-size: 1.1rem;
}

.blog-list {
  display: flex;
  flex-direction: column;
  gap: 2rem;
}

.blog-post {
  padding: 2rem;
  background: white;
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  transition: transform 0.3s, box-shadow 0.3s;
}

.blog-post:hover {
  transform: translateY(-4px);
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.15);
}

.post-title {
  margin: 0 0 1rem 0;
  color: #333;
  font-size: 1.8rem;
}

.post-meta {
  display: flex;
  gap: 1.5rem;
  margin-bottom: 1rem;
  font-size: 0.9rem;
  color: #666;
}

.post-meta span {
  display: flex;
  align-items: center;
}

.post-excerpt {
  margin: 1rem 0;
  color: #555;
  line-height: 1.6;
}

.post-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
  margin: 1rem 0;
}

.tag {
  padding: 0.25rem 0.75rem;
  background: #e3f2fd;
  color: #1976d2;
  border-radius: 16px;
  font-size: 0.85rem;
}

.read-more {
  padding: 0.75rem 1.5rem;
  background: #1976d2;
  color: white;
  border: none;
  border-radius: 6px;
  font-size: 1rem;
  cursor: pointer;
  transition: background 0.3s;
}

.read-more:hover {
  background: #1565c0;
}

.blog-stats {
  margin-top: 3rem;
  padding: 1.5rem;
  background: #f5f5f5;
  border-radius: 8px;
  text-align: center;
}

.blog-stats p {
  margin: 0.5rem 0;
  font-size: 1.1rem;
  color: #555;
}
```

---

### 🎯 Demo 2: E-commerce Product Grid (⭐⭐⭐)

```javascript
// Requirement: Product grid với filtering và categories

// Product card component
function ProductCard({
  name,
  price,
  originalPrice,
  image,
  rating,
  inStock,
  category,
}) {
  const discount = originalPrice
    ? Math.round(((originalPrice - price) / originalPrice) * 100)
    : 0;

  return (
    <div className="product-card">
      <div className="product-image-container">
        <img src={image} alt={name} className="product-image" />

        {discount > 0 && <span className="discount-badge">-{discount}%</span>}

        {!inStock && <div className="out-of-stock-overlay">Out of Stock</div>}
      </div>

      <div className="product-info">
        <span className="product-category">{category}</span>
        <h3 className="product-name">{name}</h3>

        <div className="product-rating">
          {"★".repeat(rating)}
          {"☆".repeat(5 - rating)}
        </div>

        <div className="product-pricing">
          <span className="current-price">${price}</span>
          {originalPrice && (
            <span className="original-price">${originalPrice}</span>
          )}
        </div>

        <button className="add-to-cart-btn" disabled={!inStock}>
          {inStock ? "Add to Cart" : "Unavailable"}
        </button>
      </div>
    </div>
  );
}

// Category filter component
function CategoryFilter({ categories, selectedCategory }) {
  return (
    <div className="category-filter">
      <h3>Categories</h3>
      <div className="category-buttons">
        {categories.map((category) => (
          <button
            key={category}
            className={`category-btn ${
              selectedCategory === category ? "active" : ""
            }`}
          >
            {category}
          </button>
        ))}
      </div>
    </div>
  );
}

// Main product grid
function ProductGrid() {
  const products = [
    {
      id: 1,
      name: "Wireless Headphones",
      price: 79.99,
      originalPrice: 99.99,
      image: "https://via.placeholder.com/300x300?text=Headphones",
      rating: 5,
      inStock: true,
      category: "Electronics",
    },
    {
      id: 2,
      name: "Smart Watch",
      price: 199.99,
      originalPrice: 249.99,
      image: "https://via.placeholder.com/300x300?text=Watch",
      rating: 4,
      inStock: true,
      category: "Electronics",
    },
    {
      id: 3,
      name: "Running Shoes",
      price: 89.99,
      originalPrice: null,
      image: "https://via.placeholder.com/300x300?text=Shoes",
      rating: 5,
      inStock: true,
      category: "Sports",
    },
    {
      id: 4,
      name: "Yoga Mat",
      price: 29.99,
      originalPrice: 39.99,
      image: "https://via.placeholder.com/300x300?text=Yoga+Mat",
      rating: 4,
      inStock: false,
      category: "Sports",
    },
    {
      id: 5,
      name: "Coffee Maker",
      price: 49.99,
      originalPrice: null,
      image: "https://via.placeholder.com/300x300?text=Coffee",
      rating: 5,
      inStock: true,
      category: "Home",
    },
    {
      id: 6,
      name: "Desk Lamp",
      price: 34.99,
      originalPrice: 44.99,
      image: "https://via.placeholder.com/300x300?text=Lamp",
      rating: 4,
      inStock: true,
      category: "Home",
    },
  ];

  // Get unique categories
  const allCategories = ["All", ...new Set(products.map((p) => p.category))];

  // For now, show all products (filtering sẽ học với state ở Ngày 6)
  const selectedCategory = "All";
  const filteredProducts =
    selectedCategory === "All"
      ? products
      : products.filter((p) => p.category === selectedCategory);

  // Calculate stats
  const totalProducts = filteredProducts.length;
  const inStockCount = filteredProducts.filter((p) => p.inStock).length;
  const avgPrice = (
    filteredProducts.reduce((sum, p) => sum + p.price, 0) / totalProducts
  ).toFixed(2);

  return (
    <div className="shop-container">
      <header className="shop-header">
        <h1>🛍️ Online Store</h1>
        <p>Quality products at great prices</p>
      </header>

      <div className="shop-layout">
        <aside className="sidebar">
          <CategoryFilter
            categories={allCategories}
            selectedCategory={selectedCategory}
          />

          <div className="shop-stats">
            <h3>Statistics</h3>
            <p>Total Products: {totalProducts}</p>
            <p>In Stock: {inStockCount}</p>
            <p>Average Price: ${avgPrice}</p>
          </div>
        </aside>

        <main className="product-grid">
          {filteredProducts.map((product) => (
            <ProductCard
              key={product.id}
              name={product.name}
              price={product.price}
              originalPrice={product.originalPrice}
              image={product.image}
              rating={product.rating}
              inStock={product.inStock}
              category={product.category}
            />
          ))}
        </main>
      </div>
    </div>
  );
}

export default ProductGrid;
```

---

## V. EXERCISES (90 phút)

### 💪 Exercise 1: Contact Card Component (⭐) - 15 phút

**Requirement**: Tạo reusable contact card component.

```javascript
// Starter Code:
function ContactCard() {
  // TODO: Add props parameter

  return <div>{/* TODO: Implement contact card */}</div>;
}

function App() {
  // TODO: Use ContactCard với different data
  return <div>{/* TODO: Render multiple ContactCard */}</div>;
}
```

**Your Task**:

1. Tạo `ContactCard` component nhận props: name, phone, email, company
2. Display all information trong styled card
3. Show email icon (📧) và phone icon (📱)
4. Trong `App`, render 3 ContactCards với different data
5. Add conditional: if no company, hiển thị "Freelancer"

**Expected Output**:

```
╔══════════════════════╗
║ John Doe             ║
║ 📱 +1-234-567-8900  ║
║ 📧 john@example.com ║
║ 🏢 Tech Corp        ║
╚══════════════════════╝
```

<details>
<summary>💡 Solution</summary>

```javascript
// ContactCard component
function ContactCard({ name, phone, email, company }) {
  return (
    <div className="contact-card">
      <h3 className="contact-name">{name}</h3>

      <div className="contact-details">
        <div className="contact-item">
          <span className="icon">📱</span>
          <span>{phone}</span>
        </div>

        <div className="contact-item">
          <span className="icon">📧</span>
          <span>{email}</span>
        </div>

        <div className="contact-item">
          <span className="icon">🏢</span>
          <span>{company || "Freelancer"}</span>
        </div>
      </div>
    </div>
  );
}

// App component
function App() {
  return (
    <div className="app">
      <h1>My Contacts</h1>

      <div className="contacts-grid">
        <ContactCard
          name="John Doe"
          phone="+1-234-567-8900"
          email="john@example.com"
          company="Tech Corp"
        />

        <ContactCard
          name="Jane Smith"
          phone="+1-234-567-8901"
          email="jane@example.com"
          company="Design Studio"
        />

        <ContactCard
          name="Bob Johnson"
          phone="+1-234-567-8902"
          email="bob@example.com"
          company="" // Empty - will show "Freelancer"
        />
      </div>
    </div>
  );
}

export default App;
```

**CSS**:

```css
.app {
  max-width: 1200px;
  margin: 2rem auto;
  padding: 2rem;
}

.app h1 {
  text-align: center;
  margin-bottom: 2rem;
}

.contacts-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 1.5rem;
}

.contact-card {
  padding: 1.5rem;
  background: white;
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

.contact-name {
  margin: 0 0 1rem 0;
  font-size: 1.5rem;
  color: #333;
}

.contact-details {
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
}

.contact-item {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  color: #555;
}

.icon {
  font-size: 1.2rem;
}
```

**Key Points**:

- Props destructuring cho clean code
- Conditional rendering cho company
- Reusable component
- Grid layout responsive
</details>

---

### 💪 Exercise 2: Recipe Card với Ingredients List (⭐⭐) - 25 phút

**Requirement**: Tạo recipe card component với list rendering.

```javascript
// Starter Code:
function RecipeCard() {
  // TODO: Add props

  return <div>{/* TODO: Implement recipe card */}</div>;
}
```

**Your Task**:

1. Tạo `RecipeCard` component với props:

   - title (string)
   - prepTime (number, in minutes)
   - difficulty (string: 'Easy', 'Medium', 'Hard')
   - ingredients (array of strings)
   - instructions (array of strings)

2. Display recipe information
3. Render ingredients list sử dụng `.map()`
4. Render numbered instructions sử dụng `.map()`
5. Color-code difficulty:

   - Easy: Green
   - Medium: Orange
   - Hard: Red

6. Trong App, tạo 2 different recipes

**Expected Output**:

```
╔═══════════════════════════════╗
║ Chocolate Chip Cookies        ║
║ ⏱️ 30 min | 🎯 Easy          ║
║                               ║
║ Ingredients:                  ║
║ • Flour                       ║
║ • Sugar                       ║
║ • Chocolate chips             ║
║                               ║
║ Instructions:                 ║
║ 1. Mix dry ingredients        ║
║ 2. Add wet ingredients        ║
║ 3. Bake for 15 minutes        ║
╚═══════════════════════════════╝
```

<details>
<summary>💡 Solution</summary>

```javascript
// RecipeCard component
function RecipeCard({
  title,
  prepTime,
  difficulty,
  ingredients,
  instructions,
}) {
  // Determine difficulty color
  const getDifficultyColor = () => {
    const colors = {
      Easy: "#28a745",
      Medium: "#ffc107",
      Hard: "#dc3545",
    };
    return colors[difficulty] || "#6c757d";
  };

  return (
    <div className="recipe-card">
      <h2 className="recipe-title">{title}</h2>

      <div className="recipe-meta">
        <span className="prep-time">⏱️ {prepTime} min</span>
        <span className="difficulty" style={{ color: getDifficultyColor() }}>
          🎯 {difficulty}
        </span>
      </div>

      <div className="recipe-section">
        <h3>Ingredients:</h3>
        <ul className="ingredients-list">
          {ingredients.map((ingredient, index) => (
            <li key={index}>{ingredient}</li>
          ))}
        </ul>
      </div>

      <div className="recipe-section">
        <h3>Instructions:</h3>
        <ol className="instructions-list">
          {instructions.map((instruction, index) => (
            <li key={index}>{instruction}</li>
          ))}
        </ol>
      </div>
    </div>
  );
}

// App component
function App() {
  const recipes = [
    {
      id: 1,
      title: "Chocolate Chip Cookies",
      prepTime: 30,
      difficulty: "Easy",
      ingredients: [
        "2 cups all-purpose flour",
        "1 cup butter, softened",
        "3/4 cup granulated sugar",
        "2 eggs",
        "2 cups chocolate chips",
      ],
      instructions: [
        "Preheat oven to 375°F (190°C)",
        "Mix butter and sugar until fluffy",
        "Beat in eggs one at a time",
        "Gradually blend in flour",
        "Stir in chocolate chips",
        "Drop rounded tablespoons onto baking sheet",
        "Bake for 9-11 minutes",
      ],
    },
    {
      id: 2,
      title: "Spaghetti Carbonara",
      prepTime: 45,
      difficulty: "Medium",
      ingredients: [
        "400g spaghetti",
        "200g pancetta",
        "4 egg yolks",
        "100g Parmesan cheese",
        "Black pepper to taste",
      ],
      instructions: [
        "Cook spaghetti according to package directions",
        "Fry pancetta until crispy",
        "Mix egg yolks with grated Parmesan",
        "Drain pasta, reserving 1 cup pasta water",
        "Toss hot pasta with pancetta",
        "Remove from heat and stir in egg mixture",
        "Add pasta water to reach desired consistency",
        "Season with black pepper and serve",
      ],
    },
  ];

  return (
    <div className="app">
      <header className="app-header">
        <h1>👨‍🍳 Recipe Collection</h1>
        <p>Delicious recipes for every occasion</p>
      </header>

      <div className="recipes-grid">
        {recipes.map((recipe) => (
          <RecipeCard
            key={recipe.id}
            title={recipe.title}
            prepTime={recipe.prepTime}
            difficulty={recipe.difficulty}
            ingredients={recipe.ingredients}
            instructions={recipe.instructions}
          />
        ))}
      </div>
    </div>
  );
}

export default App;
```

**CSS**:

```css
.app {
  max-width: 1200px;
  margin: 2rem auto;
  padding: 2rem;
  background: #f8f9fa;
}

.app-header {
  text-align: center;
  margin-bottom: 3rem;
}

.app-header h1 {
  margin: 0 0 0.5rem 0;
  color: #333;
}

.app-header p {
  margin: 0;
  color: #666;
}

.recipes-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
  gap: 2rem;
}

.recipe-card {
  padding: 2rem;
  background: white;
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

.recipe-title {
  margin: 0 0 1rem 0;
  color: #333;
  font-size: 1.8rem;
}

.recipe-meta {
  display: flex;
  gap: 2rem;
  margin-bottom: 1.5rem;
  padding-bottom: 1rem;
  border-bottom: 2px solid #e9ecef;
  font-size: 1rem;
}

.prep-time,
.difficulty {
  display: flex;
  align-items: center;
  font-weight: 500;
}

.recipe-section {
  margin-bottom: 1.5rem;
}

.recipe-section h3 {
  margin: 0 0 0.75rem 0;
  color: #495057;
  font-size: 1.2rem;
}

.ingredients-list,
.instructions-list {
  margin: 0;
  padding-left: 1.5rem;
}

.ingredients-list li,
.instructions-list li {
  margin-bottom: 0.5rem;
  color: #555;
  line-height: 1.6;
}
```

**Key Points**:

- Array of objects passed as props
- List rendering với `.map()`
- Index as key (OK cho static lists)
- Dynamic styling based on props
- Organized data structure
</details>

---

### 💪 Exercise 3: Team Member Directory (⭐⭐⭐) - 40 phút

**Requirement**: Build team directory với filtering và stats.

**Your Task**:

1. Tạo `TeamMember` component với props:

   - name, role, department, email, phone, avatar (URL)
   - skills (array of strings)
   - availability (boolean)

2. Tạo `DepartmentFilter` component

   - Show buttons cho each department
   - Show "All" button

3. Tạo `TeamStats` component

   - Total members
   - Available members
   - Members by department

4. Trong `App`:

   - Create array of 6+ team members
   - Render all members (filtering với state sẽ học Ngày 6)
   - Show stats
   - Include multiple departments: Engineering, Design, Marketing

5. Features:
   - Color-code availability (green dot = available, gray = busy)
   - Skills as tags
   - Responsive grid layout

<details>
<summary>💡 Solution</summary>

```javascript
// TeamMember component
function TeamMember({
  name,
  role,
  department,
  email,
  phone,
  avatar,
  skills,
  availability,
}) {
  return (
    <div className="team-member-card">
      <div className="member-header">
        <img src={avatar} alt={name} className="member-avatar" />
        <div
          className={`availability-dot ${availability ? "available" : "busy"}`}
          title={availability ? "Available" : "Busy"}
        />
      </div>

      <div className="member-info">
        <h3 className="member-name">{name}</h3>
        <p className="member-role">{role}</p>
        <span className="member-department">{department}</span>
      </div>

      <div className="member-contact">
        <div className="contact-item">
          <span>📧</span>
          <span>{email}</span>
        </div>
        <div className="contact-item">
          <span>📱</span>
          <span>{phone}</span>
        </div>
      </div>

      <div className="member-skills">
        <h4>Skills:</h4>
        <div className="skills-list">
          {skills.map((skill, index) => (
            <span key={index} className="skill-tag">
              {skill}
            </span>
          ))}
        </div>
      </div>
    </div>
  );
}

// DepartmentFilter component
function DepartmentFilter({ departments, currentDepartment }) {
  return (
    <div className="department-filter">
      <h3>Filter by Department</h3>
      <div className="filter-buttons">
        {departments.map((dept) => (
          <button
            key={dept}
            className={`filter-btn ${
              currentDepartment === dept ? "active" : ""
            }`}
          >
            {dept}
          </button>
        ))}
      </div>
    </div>
  );
}

// TeamStats component
function TeamStats({ members }) {
  const totalMembers = members.length;
  const availableMembers = members.filter((m) => m.availability).length;

  // Count by department
  const byDepartment = members.reduce((acc, member) => {
    acc[member.department] = (acc[member.department] || 0) + 1;
    return acc;
  }, {});

  return (
    <div className="team-stats">
      <h3>Team Statistics</h3>

      <div className="stat-item">
        <span className="stat-label">Total Members:</span>
        <span className="stat-value">{totalMembers}</span>
      </div>

      <div className="stat-item">
        <span className="stat-label">Available Now:</span>
        <span className="stat-value available">{availableMembers}</span>
      </div>

      <div className="stat-section">
        <h4>By Department:</h4>
        {Object.entries(byDepartment).map(([dept, count]) => (
          <div key={dept} className="stat-item">
            <span className="stat-label">{dept}:</span>
            <span className="stat-value">{count}</span>
          </div>
        ))}
      </div>
    </div>
  );
}

// Main App
function App() {
  const teamMembers = [
    {
      id: 1,
      name: "Alice Johnson",
      role: "Senior Frontend Developer",
      department: "Engineering",
      email: "alice@company.com",
      phone: "+1-234-567-8901",
      avatar: "https://i.pravatar.cc/150?img=1",
      skills: ["React", "TypeScript", "CSS"],
      availability: true,
    },
    {
      id: 2,
      name: "Bob Smith",
      role: "Backend Developer",
      department: "Engineering",
      email: "bob@company.com",
      phone: "+1-234-567-8902",
      avatar: "https://i.pravatar.cc/150?img=2",
      skills: ["Node.js", "Python", "MongoDB"],
      availability: false,
    },
    {
      id: 3,
      name: "Carol Williams",
      role: "UI/UX Designer",
      department: "Design",
      email: "carol@company.com",
      phone: "+1-234-567-8903",
      avatar: "https://i.pravatar.cc/150?img=3",
      skills: ["Figma", "Sketch", "Prototyping"],
      availability: true,
    },
    {
      id: 4,
      name: "David Brown",
      role: "Marketing Manager",
      department: "Marketing",
      email: "david@company.com",
      phone: "+1-234-567-8904",
      avatar: "https://i.pravatar.cc/150?img=4",
      skills: ["SEO", "Content Strategy", "Analytics"],
      availability: true,
    },
    {
      id: 5,
      name: "Eva Martinez",
      role: "Product Designer",
      department: "Design",
      email: "eva@company.com",
      phone: "+1-234-567-8905",
      avatar: "https://i.pravatar.cc/150?img=5",
      skills: ["Design Systems", "User Research", "Figma"],
      availability: false,
    },
    {
      id: 6,
      name: "Frank Davis",
      role: "DevOps Engineer",
      department: "Engineering",
      email: "frank@company.com",
      phone: "+1-234-567-8906",
      avatar: "https://i.pravatar.cc/150?img=6",
      skills: ["Docker", "Kubernetes", "AWS"],
      availability: true,
    },
  ];

  // Get unique departments
  const departments = ["All", ...new Set(teamMembers.map((m) => m.department))];

  // For now show all (filtering với state học Ngày 6)
  const currentDepartment = "All";
  const filteredMembers =
    currentDepartment === "All"
      ? teamMembers
      : teamMembers.filter((m) => m.department === currentDepartment);

  return (
    <div className="app">
      <header className="app-header">
        <h1>👥 Team Directory</h1>
        <p>Meet our amazing team</p>
      </header>

      <div className="directory-layout">
        <aside className="sidebar">
          <DepartmentFilter
            departments={departments}
            currentDepartment={currentDepartment}
          />

          <TeamStats members={filteredMembers} />
        </aside>

        <main className="team-grid">
          {filteredMembers.map((member) => (
            <TeamMember
              key={member.id}
              name={member.name}
              role={member.role}
              department={member.department}
              email={member.email}
              phone={member.phone}
              avatar={member.avatar}
              skills={member.skills}
              availability={member.availability}
            />
          ))}
        </main>
      </div>
    </div>
  );
}

export default App;
```

**CSS**:

```css
.app {
  max-width: 1400px;
  margin: 2rem auto;
  padding: 2rem;
}

.app-header {
  text-align: center;
  margin-bottom: 3rem;
}

.app-header h1 {
  margin: 0 0 0.5rem 0;
  font-size: 2.5rem;
  color: #333;
}

.app-header p {
  margin: 0;
  color: #666;
  font-size: 1.1rem;
}

.directory-layout {
  display: grid;
  grid-template-columns: 280px 1fr;
  gap: 2rem;
}

.sidebar {
  display: flex;
  flex-direction: column;
  gap: 2rem;
}

.department-filter,
.team-stats {
  padding: 1.5rem;
  background: white;
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

.department-filter h3,
.team-stats h3 {
  margin: 0 0 1rem 0;
  color: #333;
  font-size: 1.2rem;
}

.filter-buttons {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

.filter-btn {
  padding: 0.75rem;
  background: #f8f9fa;
  border: 2px solid transparent;
  border-radius: 8px;
  cursor: pointer;
  transition: all 0.3s;
  font-size: 0.95rem;
}

.filter-btn:hover {
  background: #e9ecef;
}

.filter-btn.active {
  background: #007bff;
  color: white;
  border-color: #0056b3;
}

.stat-item {
  display: flex;
  justify-content: space-between;
  margin-bottom: 0.75rem;
  padding: 0.5rem 0;
  border-bottom: 1px solid #e9ecef;
}

.stat-label {
  color: #666;
}

.stat-value {
  font-weight: bold;
  color: #333;
}

.stat-value.available {
  color: #28a745;
}

.stat-section {
  margin-top: 1.5rem;
}

.stat-section h4 {
  margin: 0 0 0.75rem 0;
  color: #495057;
  font-size: 1rem;
}

.team-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
  gap: 1.5rem;
}

.team-member-card {
  padding: 1.5rem;
  background: white;
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  transition: transform 0.3s, box-shadow 0.3s;
}

.team-member-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.15);
}

.member-header {
  position: relative;
  text-align: center;
  margin-bottom: 1rem;
}

.member-avatar {
  width: 100px;
  height: 100px;
  border-radius: 50%;
  object-fit: cover;
}

.availability-dot {
  position: absolute;
  bottom: 5px;
  right: calc(50% - 50px);
  width: 16px;
  height: 16px;
  border-radius: 50%;
  border: 3px solid white;
}

.availability-dot.available {
  background: #28a745;
}

.availability-dot.busy {
  background: #6c757d;
}

.member-info {
  text-align: center;
  margin-bottom: 1rem;
  padding-bottom: 1rem;
  border-bottom: 1px solid #e9ecef;
}

.member-name {
  margin: 0 0 0.25rem 0;
  color: #333;
  font-size: 1.2rem;
}

.member-role {
  margin: 0 0 0.5rem 0;
  color: #666;
  font-size: 0.9rem;
}

.member-department {
  display: inline-block;
  padding: 0.25rem 0.75rem;
  background: #e3f2fd;
  color: #1976d2;
  border-radius: 12px;
  font-size: 0.85rem;
}

.member-contact {
  margin-bottom: 1rem;
}

.contact-item {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  margin-bottom: 0.5rem;
  font-size: 0.9rem;
  color: #555;
}

.member-skills h4 {
  margin: 0 0 0.5rem 0;
  color: #495057;
  font-size: 0.95rem;
}

.skills-list {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
}

.skill-tag {
  padding: 0.25rem 0.75rem;
  background: #f8f9fa;
  border: 1px solid #dee2e6;
  border-radius: 16px;
  font-size: 0.85rem;
  color: #495057;
}

@media (max-width: 768px) {
  .directory-layout {
    grid-template-columns: 1fr;
  }

  .team-grid {
    grid-template-columns: 1fr;
  }
}
```

**Key Points**:

- Complex component composition
- Props destructuring
- List rendering with nested maps
- Computed values (stats)
- Conditional styling
- Responsive layout
- Real avatar service (pravatar.cc)
</details>

---

### 💪 Exercise 4: Dashboard với Multiple Lists (⭐⭐⭐⭐) - 60 phút

**Requirement**: Build comprehensive dashboard với notifications, tasks, và activities.

**Your Task**:

1. Tạo 3 main components:

   - `NotificationList` - Recent notifications
   - `TaskList` - Todo tasks
   - `ActivityFeed` - Recent activities

2. Each notification has:

   - type ('info', 'success', 'warning', 'error')
   - title, message, timestamp, isRead

3. Each task has:

   - title, priority ('Low', 'Medium', 'High'), completed, dueDate

4. Each activity has:

   - user, action, target, timestamp

5. Features:
   - Color-coded notification types
   - Priority badges for tasks
   - Show completed vs incomplete tasks count
   - Timeline-style activity feed
   - Summary stats for each section

<details>
<summary>💡 Solution</summary>

```javascript
// Notification component
function Notification({ type, title, message, timestamp, isRead }) {
  const getIcon = () => {
    const icons = {
      info: "ℹ️",
      success: "✅",
      warning: "⚠️",
      error: "❌",
    };
    return icons[type] || "ℹ️";
  };

  const getTypeClass = () => `notification notification-${type}`;

  return (
    <div className={getTypeClass()}>
      <div className="notification-header">
        <span className="notification-icon">{getIcon()}</span>
        <div className="notification-content">
          <h4 className="notification-title">{title}</h4>
          <p className="notification-message">{message}</p>
        </div>
        {!isRead && <span className="unread-badge">New</span>}
      </div>
      <span className="notification-time">{timestamp}</span>
    </div>
  );
}

// NotificationList component
function NotificationList({ notifications }) {
  const unreadCount = notifications.filter((n) => !n.isRead).length;

  return (
    <div className="section">
      <div className="section-header">
        <h2>🔔 Notifications</h2>
        {unreadCount > 0 && <span className="badge">{unreadCount} new</span>}
      </div>

      <div className="notifications-container">
        {notifications.map((notification) => (
          <Notification
            key={notification.id}
            type={notification.type}
            title={notification.title}
            message={notification.message}
            timestamp={notification.timestamp}
            isRead={notification.isRead}
          />
        ))}
      </div>
    </div>
  );
}

// Task component
function Task({ title, priority, completed, dueDate }) {
  const getPriorityColor = () => {
    const colors = {
      Low: "#28a745",
      Medium: "#ffc107",
      High: "#dc3545",
    };
    return colors[priority] || "#6c757d";
  };

  return (
    <div className={`task ${completed ? "completed" : ""}`}>
      <div className="task-main">
        <span className="task-checkbox">{completed ? "☑️" : "⬜"}</span>
        <div className="task-info">
          <h4 className="task-title">{title}</h4>
          <div className="task-meta">
            <span
              className="priority-badge"
              style={{ backgroundColor: getPriorityColor() }}
            >
              {priority}
            </span>
            <span className="due-date">Due: {dueDate}</span>
          </div>
        </div>
      </div>
    </div>
  );
}

// TaskList component
function TaskList({ tasks }) {
  const completedCount = tasks.filter((t) => t.completed).length;
  const totalCount = tasks.length;
  const completionRate = ((completedCount / totalCount) * 100).toFixed(0);

  return (
    <div className="section">
      <div className="section-header">
        <h2>📋 Tasks</h2>
        <span className="badge">
          {completedCount}/{totalCount} completed
        </span>
      </div>

      <div className="progress-bar-container">
        <div className="progress-bar" style={{ width: `${completionRate}%` }} />
      </div>

      <div className="tasks-container">
        {tasks.map((task) => (
          <Task
            key={task.id}
            title={task.title}
            priority={task.priority}
            completed={task.completed}
            dueDate={task.dueDate}
          />
        ))}
      </div>
    </div>
  );
}

// Activity component
function Activity({ user, action, target, timestamp }) {
  return (
    <div className="activity">
      <div className="activity-timeline-dot" />
      <div className="activity-content">
        <p className="activity-text">
          <strong>{user}</strong> {action} <em>{target}</em>
        </p>
        <span className="activity-time">{timestamp}</span>
      </div>
    </div>
  );
}

// ActivityFeed component
function ActivityFeed({ activities }) {
  return (
    <div className="section">
      <div className="section-header">
        <h2>📊 Recent Activity</h2>
        <span className="badge">{activities.length} events</span>
      </div>

      <div className="activity-feed">
        {activities.map((activity) => (
          <Activity
            key={activity.id}
            user={activity.user}
            action={activity.action}
            target={activity.target}
            timestamp={activity.timestamp}
          />
        ))}
      </div>
    </div>
  );
}

// Main Dashboard
function Dashboard() {
  const notifications = [
    {
      id: 1,
      type: "success",
      title: "Deployment Successful",
      message: "Your application has been deployed to production",
      timestamp: "5 mins ago",
      isRead: false,
    },
    {
      id: 2,
      type: "warning",
      title: "High Memory Usage",
      message: "Server memory usage is above 80%",
      timestamp: "15 mins ago",
      isRead: false,
    },
    {
      id: 3,
      type: "info",
      title: "New Comment",
      message: "Alice commented on your pull request",
      timestamp: "1 hour ago",
      isRead: true,
    },
    {
      id: 4,
      type: "error",
      title: "Build Failed",
      message: "Build #245 failed with 3 errors",
      timestamp: "2 hours ago",
      isRead: true,
    },
  ];

  const tasks = [
    {
      id: 1,
      title: "Review pull request #123",
      priority: "High",
      completed: false,
      dueDate: "Today",
    },
    {
      id: 2,
      title: "Update documentation",
      priority: "Medium",
      completed: true,
      dueDate: "Yesterday",
    },
    {
      id: 3,
      title: "Fix bug in authentication",
      priority: "High",
      completed: false,
      dueDate: "Tomorrow",
    },
    {
      id: 4,
      title: "Prepare for team meeting",
      priority: "Low",
      completed: true,
      dueDate: "Today",
    },
    {
      id: 5,
      title: "Code review for feature branch",
      priority: "Medium",
      completed: false,
      dueDate: "Tomorrow",
    },
  ];

  const activities = [
    {
      id: 1,
      user: "John Doe",
      action: "merged",
      target: "pull request #123",
      timestamp: "10 mins ago",
    },
    {
      id: 2,
      user: "Jane Smith",
      action: "commented on",
      target: "issue #456",
      timestamp: "25 mins ago",
    },
    {
      id: 3,
      user: "Bob Johnson",
      action: "created",
      target: "new branch feature/login",
      timestamp: "1 hour ago",
    },
    {
      id: 4,
      user: "Alice Williams",
      action: "deployed",
      target: "v2.3.0 to production",
      timestamp: "2 hours ago",
    },
    {
      id: 5,
      user: "Charlie Brown",
      action: "closed",
      target: "issue #789",
      timestamp: "3 hours ago",
    },
  ];

  return (
    <div className="dashboard">
      <header className="dashboard-header">
        <h1>📊 Dashboard</h1>
        <p>Welcome back! Here's what's happening</p>
      </header>

      <div className="dashboard-grid">
        <div className="dashboard-column">
          <NotificationList notifications={notifications} />
          <TaskList tasks={tasks} />
        </div>

        <div className="dashboard-column">
          <ActivityFeed activities={activities} />
        </div>
      </div>
    </div>
  );
}

export default Dashboard;
```

**CSS**:

```css
.dashboard {
  max-width: 1400px;
  margin: 2rem auto;
  padding: 2rem;
  background: #f8f9fa;
}

.dashboard-header {
  text-align: center;
  margin-bottom: 3rem;
}

.dashboard-header h1 {
  margin: 0 0 0.5rem 0;
  font-size: 2.5rem;
  color: #333;
}

.dashboard-header p {
  margin: 0;
  color: #666;
  font-size: 1.1rem;
}

.dashboard-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 2rem;
}

.dashboard-column {
  display: flex;
  flex-direction: column;
  gap: 2rem;
}

.section {
  background: white;
  border-radius: 12px;
  padding: 1.5rem;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

.section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 1.5rem;
  padding-bottom: 1rem;
  border-bottom: 2px solid #e9ecef;
}

.section-header h2 {
  margin: 0;
  color: #333;
  font-size: 1.5rem;
}

.badge {
  padding: 0.25rem 0.75rem;
  background: #007bff;
  color: white;
  border-radius: 12px;
  font-size: 0.85rem;
  font-weight: 500;
}

/* Notifications */
.notifications-container {
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.notification {
  padding: 1rem;
  border-radius: 8px;
  border-left: 4px solid;
}

.notification-info {
  background: #e7f3ff;
  border-left-color: #2196f3;
}

.notification-success {
  background: #e8f5e9;
  border-left-color: #4caf50;
}

.notification-warning {
  background: #fff3e0;
  border-left-color: #ff9800;
}

.notification-error {
  background: #ffebee;
  border-left-color: #f44336;
}

.notification-header {
  display: flex;
  align-items: flex-start;
  gap: 1rem;
  margin-bottom: 0.5rem;
}

.notification-icon {
  font-size: 1.5rem;
}

.notification-content {
  flex: 1;
}

.notification-title {
  margin: 0 0 0.25rem 0;
  color: #333;
  font-size: 1rem;
}

.notification-message {
  margin: 0;
  color: #666;
  font-size: 0.9rem;
}

.unread-badge {
  padding: 0.25rem 0.5rem;
  background: #007bff;
  color: white;
  border-radius: 8px;
  font-size: 0.75rem;
}

.notification-time {
  font-size: 0.85rem;
  color: #999;
}

/* Tasks */
.progress-bar-container {
  width: 100%;
  height: 8px;
  background: #e9ecef;
  border-radius: 4px;
  margin-bottom: 1rem;
  overflow: hidden;
}

.progress-bar {
  height: 100%;
  background: linear-gradient(90deg, #28a745, #20c997);
  transition: width 0.3s ease;
}

.tasks-container {
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
}

.task {
  padding: 1rem;
  background: #f8f9fa;
  border-radius: 8px;
  border: 2px solid transparent;
  transition: all 0.3s;
}

.task:hover {
  border-color: #007bff;
}

.task.completed {
  opacity: 0.6;
}

.task.completed .task-title {
  text-decoration: line-through;
}

.task-main {
  display: flex;
  gap: 1rem;
  align-items: flex-start;
}

.task-checkbox {
  font-size: 1.5rem;
}

.task-info {
  flex: 1;
}

.task-title {
  margin: 0 0 0.5rem 0;
  color: #333;
  font-size: 1rem;
}

.task-meta {
  display: flex;
  gap: 1rem;
  align-items: center;
}

.priority-badge {
  padding: 0.25rem 0.5rem;
  color: white;
  border-radius: 4px;
  font-size: 0.75rem;
  font-weight: 600;
}

.due-date {
  font-size: 0.85rem;
  color: #666;
}

/* Activity Feed */
.activity-feed {
  position: relative;
}

.activity-feed::before {
  content: "";
  position: absolute;
  left: 12px;
  top: 0;
  bottom: 0;
  width: 2px;
  background: #e9ecef;
}

.activity {
  position: relative;
  padding-left: 3rem;
  margin-bottom: 1.5rem;
}

.activity:last-child {
  margin-bottom: 0;
}

.activity-timeline-dot {
  position: absolute;
  left: 6px;
  top: 4px;
  width: 14px;
  height: 14px;
  background: #007bff;
  border: 3px solid white;
  border-radius: 50%;
  box-shadow: 0 0 0 2px #007bff;
}

.activity-content {
  padding: 0.75rem;
  background: #f8f9fa;
  border-radius: 8px;
}

.activity-text {
  margin: 0 0 0.5rem 0;
  color: #333;
  font-size: 0.95rem;
}

.activity-text strong {
  color: #007bff;
}

.activity-text em {
  color: #666;
  font-style: normal;
  font-weight: 500;
}

.activity-time {
  font-size: 0.85rem;
  color: #999;
}

@media (max-width: 768px) {
  .dashboard-grid {
    grid-template-columns: 1fr;
  }
}
```

**Key Points**:

- Multiple component types
- Complex data structures
- Computed stats and percentages
- Color-coded elements
- Timeline visualization
- Responsive grid layout
- Professional dashboard design
</details>

---

## VI. DEBUG LAB (20 phút)

### 🐛 Bug 1: Missing Key Prop

```javascript
// This component has missing key warning. Find and fix it.

function UserList() {
  const users = [
    { id: 1, name: "John" },
    { id: 2, name: "Jane" },
    { id: 3, name: "Bob" },
  ];

  return (
    <div>
      {users.map((user) => (
        <div>
          <h3>{user.name}</h3>
        </div>
      ))}
    </div>
  );
}
```

<details>
<summary>💡 Solution & Explanation</summary>

```javascript
// ✅ Fixed version:

function UserList() {
  const users = [
    { id: 1, name: "John" },
    { id: 2, name: "Jane" },
    { id: 3, name: "Bob" },
  ];

  return (
    <div>
      {users.map((user) => (
        <div key={user.id}>
          {" "}
          {/* Added key prop */}
          <h3>{user.name}</h3>
        </div>
      ))}
    </div>
  );
}
```

**Issues Fixed**:

1. **Missing key prop**: React needs keys để identify elements trong list
2. **Why keys matter**: Giúp React track changes, additions, removals efficiently
3. **Key placement**: Phải ở outermost element trong map callback

**Warning bạn sẽ thấy**:

```
Warning: Each child in a list should have a unique "key" prop.
```

**Best practices**:

- Use stable, unique IDs (như database IDs)
- Avoid using array index nếu list có thể reorder
- Key phải unique trong siblings (không cần globally unique)
</details>

---

### 🐛 Bug 2: Props Not Destructured Properly

```javascript
// This component has props destructuring errors. Find and fix them.

function ProductCard(name, price, inStock) {
  return (
    <div className="product">
      <h3>{name}</h3>
      <p>${price}</p>
      {inStock && <button>Add to Cart</button>}
    </div>
  );
}

// Usage
function App() {
  return <ProductCard name="Laptop" price={999} inStock={true} />;
}
```

<details>
<summary>💡 Solution & Explanation</summary>

```javascript
// ✅ Fixed version:

// Option 1: Destructure trong parameters (recommended)
function ProductCard({ name, price, inStock }) {
  return (
    <div className="product">
      <h3>{name}</h3>
      <p>${price}</p>
      {inStock && <button>Add to Cart</button>}
    </div>
  );
}

// Option 2: Accept props object
function ProductCard(props) {
  return (
    <div className="product">
      <h3>{props.name}</h3>
      <p>${props.price}</p>
      {props.inStock && <button>Add to Cart</button>}
    </div>
  );
}

// Option 3: Destructure trong body
function ProductCard(props) {
  const { name, price, inStock } = props;

  return (
    <div className="product">
      <h3>{name}</h3>
      <p>${price}</p>
      {inStock && <button>Add to Cart</button>}
    </div>
  );
}

// Usage (same for all)
function App() {
  return <ProductCard name="Laptop" price={999} inStock={true} />;
}
```

**Issues Fixed**:

1. **Wrong parameter format**: Components nhận một `props` object, không phải individual parameters
2. **Props structure**: React wraps all attributes vào một props object
3. **Destructuring**: Cần destructure từ props object

**Why the original code fails**:

- Function receives: `{ name: 'Laptop', price: 999, inStock: true }`
- But tries to use as: `name, price, inStock` (3 separate arguments)
- Result: `name` has whole props object, `price` và `inStock` are undefined
</details>

---

### 🐛 Bug 3: Children Prop Misuse

```javascript
// This component doesn't handle children properly. Find and fix it.

function Card({ title, description }) {
  return (
    <div className="card">
      <h2>{title}</h2>
      <p>{description}</p>
    </div>
  );
}

// Usage - trying to pass children
function App() {
  return (
    <Card title="Welcome">
      <p>This is the card content</p>
      <button>Click me</button>
    </Card>
  );
}

// Expected: Content và button nên hiển thị
// Actual: Chỉ có title, content và button bị ignored
```

<details>
<summary>💡 Solution & Explanation</summary>

```javascript
// ✅ Fixed version:

function Card({ title, description, children }) {
  return (
    <div className="card">
      <h2>{title}</h2>
      {description && <p>{description}</p>}
      <div className="card-content">{children}</div>
    </div>
  );
}

// Usage
function App() {
  return (
    <Card title="Welcome">
      <p>This is the card content</p>
      <button>Click me</button>
    </Card>
  );
}

// Or với description:
function App() {
  return (
    <Card title="Welcome" description="A nice card">
      <p>This is the card content</p>
      <button>Click me</button>
    </Card>
  );
}
```

**Issues Fixed**:

1. **Missing children prop**: Component không accept `children` trong props
2. **Not rendering children**: Không render `{children}` trong JSX
3. **Optional description**: Made description optional với conditional rendering

**Key concepts**:

- **Children prop**: Special prop chứa content giữa opening/closing tags
- **Flexibility**: Children cho phép flexible content composition
- **Always named `children`**: React convention, always use this name

**When to use children vs props**:

```javascript
// Use props cho: Simple, known data
<Card title="..." description="..." />

// Use children cho: Complex, variable content
<Card title="...">
  <div>Any JSX here</div>
  <Component />
  <p>Mix of elements</p>
</Card>
```

</details>

---

## VII. INTERVIEW PREP (15 phút)

### 💼 Junior Level Questions

**Q1**: What are props in React and how do you pass them?

<details>
<summary>Expected Answer</summary>

**Props** (short for "properties") are how you pass data từ parent component sang child component.

**Key characteristics**:

- **Read-only**: Child component không thể modify props
- **Unidirectional**: Data flows one way (parent → child)
- **Like function arguments**: Similar to passing arguments to functions

**How to pass props**:

```javascript
// Parent component
function App() {
  return <UserCard name="John Doe" age={25} email="john@example.com" />;
}

// Child component receiving props
function UserCard(props) {
  return (
    <div>
      <h2>{props.name}</h2>
      <p>Age: {props.age}</p>
      <p>Email: {props.email}</p>
    </div>
  );
}

// Or với destructuring (cleaner):
function UserCard({ name, age, email }) {
  return (
    <div>
      <h2>{name}</h2>
      <p>Age: {age}</p>
      <p>Email: {email}</p>
    </div>
  );
}
```

**Types of data you can pass**:

- Strings: `name="John"`
- Numbers: `age={25}`
- Booleans: `active={true}` or `active`
- Arrays: `items={[1, 2, 3]}`
- Objects: `user={{ name: 'John' }}`
- Functions: `onClick={handleClick}`

**Important rules**:

1. Props are immutable (read-only)
2. All JSX attributes become props
3. Use curly braces `{}` for non-string values
</details>

---

**Q2**: Why do we need keys when rendering lists?

<details>
<summary>Expected Answer</summary>

**Keys** help React identify which items trong list đã changed, added, or removed.

**Why keys are important**:

**1. Performance optimization**:

```javascript
// Without stable keys, React may:
// - Re-render entire list unnecessarily
// - Lose component state
// - Cause UI bugs

// With stable keys:
// - Only updates changed items
// - Preserves component state
// - Efficient reconciliation
```

**2. Prevent bugs**:

```javascript
// Example: Removing middle item

// Initial list (using index as key):
[
  { id: 1, name: "A" }, // key: 0
  { id: 2, name: "B" }, // key: 1
  { id: 3, name: "C" }, // key: 2
][
  // After removing B:
  ({ id: 1, name: "A" }, // key: 0 (same)
  { id: 3, name: "C" }) // key: 1 (changed from 2!)
];

// React thinks:
// - Item at key 0 didn't change ✓
// - Item at key 1 changed from B to C ✗ (wrong!)
// - Item at key 2 was removed ✗ (wrong!)
```

**Best practices**:

```javascript
// ✅ GOOD: Use unique, stable ID
{
  users.map((user) => <UserCard key={user.id} {...user} />);
}

// ❌ BAD: Use index (can cause bugs)
{
  users.map((user, index) => <UserCard key={index} {...user} />);
}

// ⚠️ OK: Use index only if:
// - List is static (never reordered)
// - Items don't have unique IDs
// - List is simple display-only
```

**When index as key is acceptable**:

- Static list that never changes
- Simple string/number array
- No state associated with items
- Pure display (no interactions)

**Key rules**:

1. Must be unique among siblings
2. Should be stable (same item = same key)
3. Don't use random values
4. Don't use array index if list can change
</details>

---

**Q3**: What is the children prop and when should you use it?

<details>
<summary>Expected Answer</summary>

**Children prop** là special prop chứa content passed giữa opening và closing tags của component.

**Basic usage**:

```javascript
// Component definition
function Card({ children }) {
  return <div className="card">{children}</div>;
}

// Usage
<Card>
  <h2>Title</h2>
  <p>Content here</p>
</Card>;

// children receives:
// <>
//   <h2>Title</h2>
//   <p>Content here</p>
// </>
```

**When to use children**:

**1. Layout components** (wrappers):

```javascript
function Container({ children }) {
  return <div className="container">{children}</div>;
}

function Page({ children }) {
  return (
    <div>
      <Header />
      <main>{children}</main>
      <Footer />
    </div>
  );
}
```

**2. Reusable UI components**:

```javascript
function Modal({ title, children }) {
  return (
    <div className="modal">
      <h2>{title}</h2>
      <div className="modal-body">{children}</div>
    </div>
  );
}

// Flexible usage:
<Modal title="Confirm">
  <p>Are you sure?</p>
  <button>Yes</button>
  <button>No</button>
</Modal>;
```

**3. Composition patterns**:

```javascript
function Button({ variant, children }) {
  return (
    <button className={`btn btn-${variant}`}>
      {children}
    </button>
  )
}

// Can pass text, icons, or both:
<Button variant="primary">Click me</Button>
<Button variant="primary">
  <Icon name="save" /> Save
</Button>
```

**Children vs Props**:

```javascript
// Use props cho: Known, specific data
<Card title="..." description="..." />

// Use children cho: Variable, flexible content
<Card>
  <CustomComponent />
  <AnyContent />
</Card>
```

**Types of children**:

- Text: `<Component>Hello</Component>`
- Single element: `<Component><div>...</div></Component>`
- Multiple elements: `<Component><div>...</div><p>...</p></Component>`
- Components: `<Component><OtherComponent /></Component>`
- Expressions: `<Component>{condition && <div>...</div>}</Component>`

**Advanced pattern - Render props** (preview):

```javascript
<Component>{(data) => <div>{data}</div>}</Component>
```

</details>

---

### 💼 Mid Level Questions

**Q4**: Explain the difference between controlled and uncontrolled components (in context of props).

<details>
<summary>Expected Answer</summary>

**Note**: Full controlled/uncontrolled concepts sẽ học với forms và state sau. Nhưng có thể explain trong context of props.

**Controlled Component**:
Component mà value được controlled bởi parent (through props):

```javascript
// Parent controls the value
function Parent() {
  const userName = "John"; // Parent owns the data

  return <DisplayName name={userName} />;
}

function DisplayName({ name }) {
  // Component just displays the value
  // Cannot change it (props are read-only)
  return <h1>{name}</h1>;
}
```

**Uncontrolled Component**:
Component manages its own internal data:

```javascript
// Component manages own data (sẽ học với useState)
function Counter() {
  // Has its own internal count (state - Ngày 6)
  return <div>Count: ...</div>;
}
```

**In context of Day 4 (props only)**:

**Controlled by props**:

```javascript
function ToggleButton({ isOn }) {
  // Parent determines if button is on/off
  return <button className={isOn ? "on" : "off"}>{isOn ? "ON" : "OFF"}</button>;
}

// Usage:
<ToggleButton isOn={true} />; // Parent controls state
```

**Key points for interview**:

- **Controlled**: Parent component controls data via props
- **Uncontrolled**: Component has own internal state (học Ngày 6)
- **Props**: Always make component controlled by parent
- **State** (Ngày 6): Makes component uncontrolled

**Why it matters**:

- Single source of truth (controlled)
- Predictable data flow
- Easier debugging
- Better testability
</details>

---

**Q5**: How would you pass data from a grandparent to grandchild component?

<details>
<summary>Expected Answer</summary>

**Problem**: Prop drilling - passing props through intermediate components.

**Current approach (Day 4 level)**:

```javascript
// Grandparent
function App() {
  const user = { name: "John", role: "Admin" };

  return <ParentComponent user={user} />;
}

// Parent (just passes through)
function ParentComponent({ user }) {
  return (
    <div>
      <h1>Dashboard</h1>
      <ChildComponent user={user} />
    </div>
  );
}

// Grandchild (actually uses it)
function ChildComponent({ user }) {
  return (
    <div>
      {user.name} - {user.role}
    </div>
  );
}
```

**Issues với prop drilling**:

1. **Verbose**: Pass props through every level
2. **Maintenance**: Hard to refactor
3. **Coupling**: Intermediate components depend on props they don't use

**Solutions** (sẽ học sau):

**1. Component Composition** (có thể dùng ngay với Day 4 knowledge):

```javascript
// Grandparent
function App() {
  const user = { name: "John", role: "Admin" };

  // Pass child component directly
  return (
    <ParentComponent>
      <ChildComponent user={user} />
    </ParentComponent>
  );
}

// Parent doesn't need to know about user
function ParentComponent({ children }) {
  return (
    <div>
      <h1>Dashboard</h1>
      {children}
    </div>
  );
}

// Grandchild
function ChildComponent({ user }) {
  return (
    <div>
      {user.name} - {user.role}
    </div>
  );
}
```

**2. Context API** (Week 3+):

```javascript
// Will learn later - avoids prop drilling entirely
const UserContext = createContext();

function App() {
  const user = { name: "John", role: "Admin" };

  return (
    <UserContext.Provider value={user}>
      <ParentComponent />
    </UserContext.Provider>
  );
}

// Grandchild can access directly
function ChildComponent() {
  const user = useContext(UserContext);
  return <div>{user.name}</div>;
}
```

**3. State Management** (Later weeks):

- Redux, Zustand, etc.
- Global state accessible anywhere

**For now (Day 4)**:

- Use prop drilling (it's okay for 2-3 levels)
- Use component composition to reduce drilling
- Don't over-optimize early
</details>

---

### 💼 Senior Level Questions

**Q6**: Discuss performance implications of different prop passing strategies and when to optimize.

<details>
<summary>Expected Answer</summary>

**Performance considerations với props**:

**1. Prop changes trigger re-renders**:

```javascript
function Parent() {
  const user = { name: "John", age: 25 }; // New object every render!

  return <Child user={user} />;
  // Child re-renders every time, even if values same
}

// Better (sẽ học với useMemo):
function Parent() {
  const user = useMemo(
    () => ({ name: "John", age: 25 }),
    [] // Only create once
  );

  return <Child user={user} />;
}
```

**2. Inline object/array props**:

```javascript
// ❌ BAD: Creates new object every render
<Component
  style={{ color: 'red', fontSize: '16px' }}
  items={[1, 2, 3]}
/>

// ✅ BETTER: Define outside
const buttonStyle = { color: 'red', fontSize: '16px' }
const numbers = [1, 2, 3]

<Component style={buttonStyle} items={numbers} />
```

**3. Function props**:

```javascript
// ❌ BAD: New function every render
<Button onClick={() => console.log('clicked')} />

// ✅ BETTER: Define once (với useCallback sau này)
const handleClick = () => console.log('clicked')
<Button onClick={handleClick} />
```

**4. Spreading props**:

```javascript
// Can be inefficient nếu object lớn
function Component(props) {
  return <ChildComponent {...props} />;
  // Passes ALL props, even unused ones
}

// Better: Pick only needed props
function Component({ name, email, ...rest }) {
  return <ChildComponent name={name} email={email} />;
}
```

**5. Large lists với objects**:

```javascript
// ❌ EXPENSIVE: Process data trong render
function List({ items }) {
  const sortedItems = items.sort(...)  // Runs every render!

  return sortedItems.map(item => <Item key={item.id} {...item} />)
}

// ✅ BETTER: Memoize expensive operations
function List({ items }) {
  const sortedItems = useMemo(
    () => items.sort(...),
    [items]
  )

  return sortedItems.map(item => <Item key={item.id} {...item} />)
}
```

**When to optimize**:

**DON'T optimize if**:

- Component renders rarely
- Operation is cheap (<1ms)
- No noticeable lag
- Premature optimization

**DO optimize when**:

- Profiler shows bottleneck
- Large lists (1000+ items)
- Complex calculations
- Frequent re-renders
- Slow devices

**Optimization techniques** (sẽ học sau):

1. `React.memo()` - Prevent unnecessary re-renders
2. `useMemo()` - Memoize expensive calculations
3. `useCallback()` - Memoize functions
4. `useRef()` - Stable references
5. Code splitting - Load components lazily
6. Virtualization - Render only visible items

**Day 4 best practices**:

- Keep prop objects outside components when possible
- Avoid inline object/array literals in JSX
- Don't pass entire objects if only need few properties
- Use primitive values when possible
- Profile before optimizing!

**Pro tip**:

```javascript
// Use React DevTools Profiler to identify:
// 1. Which components re-render
// 2. Why they re-render (props change)
// 3. How long renders take
// Then optimize only what matters!
```

</details>

---

## VIII. CODE REVIEW CHECKLIST (15 phút)

### 📋 Components & Props Code Review Guide

**When reviewing Day 4 code, check:**

#### ✅ Component Structure

```javascript
// ❌ BAD
function myComponent() {
  // Lowercase
  return <div>content</div>;
}

// ❌ BAD
function Component() {
  const NestedComponent = () => <div>Bad</div>; // Nested definition
  return <NestedComponent />;
}

// ✅ GOOD
function MyComponent() {
  // PascalCase
  return <div>content</div>;
}

// ✅ GOOD - separate file
function NestedComponent() {
  return <div>Good</div>;
}
```

**Checklist**:

- [ ] PascalCase naming
- [ ] No nested component definitions
- [ ] Returns JSX
- [ ] Proper export (default or named)
- [ ] One component per file (recommended)

---

#### ✅ Props Handling

```javascript
// ❌ BAD
function UserCard(name, email, age) {
  // Wrong parameters
  return <div>{name}</div>;
}

// ❌ BAD
function UserCard(props) {
  props.name = "Modified"; // Mutating props!
  return <div>{props.name}</div>;
}

// ✅ GOOD
function UserCard({ name, email, age }) {
  // Destructured
  return (
    <div>
      <h2>{name}</h2>
      <p>{email}</p>
      <p>Age: {age}</p>
    </div>
  );
}

// ✅ GOOD - with defaults
function Button({ label = "Click me", disabled = false }) {
  return <button disabled={disabled}>{label}</button>;
}
```

**Checklist**:

- [ ] Props destructured (preferred) or accessed via props.x
- [ ] No prop mutations
- [ ] Default values for optional props
- [ ] Prop types documented (comments or PropTypes)

---

#### ✅ List Rendering

```javascript
// ❌ BAD
{
  users.map((user) => (
    <div>
      {" "}
      {/* Missing key */}
      <h3>{user.name}</h3>
    </div>
  ));
}

// ❌ BAD
{
  users.map((user, index) => (
    <div key={index}>
      {" "}
      {/* Index as key - anti-pattern */}
      <h3>{user.name}</h3>
    </div>
  ));
}

// ✅ GOOD
{
  users.map((user) => (
    <div key={user.id}>
      {" "}
      {/* Stable, unique ID */}
      <h3>{user.name}</h3>
    </div>
  ));
}
```

**Checklist**:

- [ ] All lists have key prop
- [ ] Keys are stable and unique
- [ ] Not using array index (unless list is static)
- [ ] Key on correct element (outermost in map)

---

#### ✅ Children Prop

```javascript
// ❌ BAD
function Card({ title, content }) {
  // No children
  return (
    <div>
      <h2>{title}</h2>
      <p>{content}</p>
    </div>
  );
}

// Usage: Can't pass flexible content
<Card title="Title" content="Fixed text only" />;

// ✅ GOOD
function Card({ title, children }) {
  return (
    <div>
      <h2>{title}</h2>
      <div>{children}</div>
    </div>
  );
}

// Usage: Flexible content
<Card title="Title">
  <p>Any content</p>
  <button>Action</button>
</Card>;
```

**Checklist**:

- [ ] Layout components use children prop
- [ ] Children prop named "children" (convention)
- [ ] Children rendered in JSX with `{children}`
- [ ] Consider children for flexible content

---

#### ✅ Component Composition

```javascript
// ❌ BAD - God component
function Dashboard() {
  return (
    <div>
      {/* 500 lines of JSX */}
      {/* Everything in one component */}
    </div>
  );
}

// ✅ GOOD - Composition
function Dashboard() {
  return (
    <div>
      <DashboardHeader />
      <DashboardStats />
      <DashboardCharts />
      <DashboardFooter />
    </div>
  );
}

// Each component is focused and reusable
```

**Checklist**:

- [ ] Components are small and focused
- [ ] Reusable components extracted
- [ ] Logical component hierarchy
- [ ] No components >200 lines (guideline)

---

#### ✅ Props Organization

```javascript
// ❌ BAD - Too many individual props
<UserProfile
  firstName="John"
  lastName="Doe"
  email="john@example.com"
  phone="+1234567890"
  street="123 Main St"
  city="New York"
  state="NY"
  country="USA"
  zipCode="10001"
/>

// ✅ GOOD - Grouped props
<UserProfile
  user={{
    firstName: "John",
    lastName: "Doe",
    email: "john@example.com",
    phone: "+1234567890"
  }}
  address={{
    street: "123 Main St",
    city: "New York",
    state: "NY",
    country: "USA",
    zipCode: "10001"
  }}
/>

// ✅ EVEN BETTER - Spread when appropriate
const user = { firstName: "John", lastName: "Doe", ... }
<UserProfile user={user} address={address} />
```

**Checklist**:

- [ ] Related props grouped into objects
- [ ] Not too many props (>5-7 is code smell)
- [ ] Consider spreading when passing many props
- [ ] Clear prop naming

---

## IX. DAILY RECAP & NEXT STEPS (10 phút)

### 🎯 Hôm nay đã học:

#### Core Concepts:

- ✅ Function components - Building blocks của React
- ✅ Props - Passing data parent → child
- ✅ Props destructuring - Clean syntax
- ✅ Children prop - Flexible composition
- ✅ List rendering - `.map()` với key prop
- ✅ Component composition - Building complex UIs

#### Key Takeaways:

1. **Components** là functions return JSX
2. **Props** are read-only - never mutate them
3. **Data flow** is unidirectional (parent → child)
4. **Keys** are essential cho list rendering
5. **Children prop** enables flexible composition
6. **Destructuring** makes code cleaner

---

### 📝 Self-Assessment Questions:

1. Làm sao pass multiple props sang component?
2. Tại sao props are read-only?
3. Khi nào dùng children prop?
4. Why keys are important trong lists?
5. Index as key - khi nào OK, khi nào không?

<details>
<summary>💡 Answers</summary>

1. **Passing multiple props**:

   ```javascript
   <Component prop1="value" prop2={123} prop3={true} />
   // Or spread:
   <Component {...propsObject} />
   ```

2. **Props are read-only because**:

   - Ensures predictable data flow
   - Parent owns the data
   - Child just renders it
   - Prevents unexpected mutations
   - Makes debugging easier

3. **Use children prop when**:

   - Creating layout/wrapper components
   - Need flexible content composition
   - Content varies significantly between uses
   - Building reusable UI patterns

4. **Keys are important because**:

   - Help React identify which items changed
   - Optimize re-rendering performance
   - Preserve component state correctly
   - Prevent UI bugs

5. **Index as key**:
   - ✅ OK when: Static list, display-only, no reordering
   - ❌ NOT OK when: List can reorder, items have state, dynamic operations
   </details>

---

### 🔗 Ngày mai sẽ học (Day 5):

#### Events & Interaction:

- **Event handling** - Responding to user actions
- **Synthetic events** - React's event system
- **Event patterns** - Common event handling patterns
- **Form basics** - Handling input (preview)
- **Component interaction** - Components talking to each other

#### Preparation:

- [ ] Review hôm nay's components & props
- [ ] Practice list rendering
- [ ] Understand prop flow clearly
- [ ] Be ready to add interactivity

---

### 📚 Additional Resources:

#### Official Docs:

- [React: Passing Props to a Component](https://react.dev/learn/passing-props-to-a-component)
- [React: Rendering Lists](https://react.dev/learn/rendering-lists)
- [React: Passing JSX as children](https://react.dev/learn/passing-props-to-a-component#passing-jsx-as-children)

#### Practice:

- [React Docs: Tic-Tac-Toe Tutorial](https://react.dev/learn/tutorial-tic-tac-toe) - Now with components!
- [Component Playground](https://codesandbox.io/s/new) - Practice online

---

## 🎉 CONGRATULATIONS!

Bạn đã hoàn thành **Ngày 4: Components & Props**!

### 📊 Progress Tracker:

```
PHASE 1: React Fundamentals (Week 1-4)
═══════════════════════════════════════
Week 1: Modern JavaScript + React Intro (Days 1-5)
├─ [████████████] 80% Complete
│  ├─ Day 1: ES6+ Essentials ✅
│  ├─ Day 2: ES6+ Advanced ✅
│  ├─ Day 3: React Basics & JSX ✅
│  ├─ Day 4: Components & Props ✅
│  └─ Day 5: Events & Conditional Rendering (Next)
```

**You're crushing it!** 🚀

---

### 💬 Final Tips:

1. **Practice Component Thinking** - Break UIs into components
2. **Props Flow Matters** - Always parent → child
3. **Keys Are Not Optional** - Always use proper keys
4. **Composition Over Complexity** - Small, focused components
5. **Read Others' Code** - Study open-source React projects

---

### ✅ Before Moving to Day 5:

Check these concepts:

- [ ] Can create functional components
- [ ] Understand how to pass and receive props
- [ ] Know when to use children prop
- [ ] Can render lists with proper keys
- [ ] Understand component composition

If any concept is unclear, review that section before Day 5!

---

**See you tomorrow for Day 5: Events & Interaction!** 👋

_Tomorrow you'll make your components interactive - handling clicks, inputs, and user actions. The fun really begins!_ 🎮
